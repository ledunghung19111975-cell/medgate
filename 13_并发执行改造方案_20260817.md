# 多维度测试集并发执行改造方案（2026-08-17）

| 项目 | 内容 |
| --- | --- |
| 背景 | MedGate 将扩充为 4 维 100–200 条测试集（见 `12_医疗测评集选型调研`），现有 live 执行为全串行，预计 1200–1800 次 DeepSeek 调用，墙钟 40 分钟–3 小时，演示与日常迭代不可行 |
| 目标 | 通过病例级并行把墙钟时间压缩到可接受范围（目标：150 条 × 2 版本，8 路并发下约 5–10 分钟），不改变调用次数、费用、评测语义与幂等语义 |
| 关联代码 | `medgate/live.py`（record_live 串行循环）、`medgate/deepseek.py`（同步 client、无重试）、`medgate/api.py`（live worker 线程 + SSE 事件）、`prototype/index.html`（SSE 进度展示） |

---

## 一、现状与瓶颈（已核实）

1. `record_live`（`live.py:436`）两层串行 for：先跑完 baseline 全部病例、再跑 candidate 全部病例；每个病例内部逐 turn 调模型，随后串行 Judge、冲突时串行复审（`live.py:558-587`）。
2. DeepSeek client 为同步 `httpx.Client`（`deepseek.py:72`），每次调用阻塞；429 时直接抛 `DEEPSEEK_RATE_LIMITED`（`deepseek.py:110`），**无退避重试**。
3. live 执行在 API 侧的单 worker 线程内（`api.py:719 _execute_live_run`），`app.state.live_lock` 保证全局单 run 互斥；SSE 事件经 `on_event` 回调推给浏览器。
4. 结论：瓶颈是"串行 + 无并发 + 无退避"三者叠加，不是单次调用本身慢。

## 二、并行设计

### 2.1 并行粒度：病例级（case-level）

- **可并行单元 = 单个病例 × 版本侧**（baseline 与 candidate 各自独立）。
- 每个并行任务跑完整链路：多轮 Agent 调用（turn 间**必须串行**，上下文依赖）→ 规则扫描 → Judge → 冲突时复审 → 产出该病例 fixture。
- **不做**同一病例多轮 turns 并行、**不做** Agent/Judge/复审同一病例内并行（有依赖），避免引入语义正确性风险。

### 2.2 并发度

- 默认 **4–8 路**，通过 `MEDGATE_LIVE_CONCURRENCY` 环境变量可配（上限 16）。
- 理由：DeepSeek 有 RPM/TPM 限流，4–8 路是保守安全的起步值；并发过高触发 429 反而因重试放大耗时与费用。
- 前端运行弹窗显示"并发 N 路"。

### 2.3 任务调度与顺序

- 用 Python `concurrent.futures.ThreadPoolExecutor(max_workers=N)` 提交全部 `case × role` 任务（150×2 = 300 个任务，线程池排队）。
- **提交顺序必须固定**为 `for role in (baseline, candidate): for case in bundle.cases`（精确复现现有串行循环顺序，`live.py:436-445`）。这是并发=1 时与串行逐字节一致的先决条件（见 §3.4）。
- 每个任务返回 `(role, case_id, fixture)`；主线程聚合后**必须按 (role, case) 双键重排**，精确复现串行顺序。
- ⚠️ **顺序影响哈希，不是"不影响"**：`canonical_hash`（`engine.py:121-123`）的 `json.dumps(sort_keys=True)` 只排 dict 键、不排 list 元素；`record_live` 的 `fixture_hash=canonical_hash(fixtures)`（`live.py:624`）与报告 `evaluations` 都依赖列表顺序。聚合顺序错一次，fixture_hash / run_input_hash / 报告快照哈希全部改变。
- `call_count` 用原子计数器（`threading.Lock`）统计；预算与取消检查下沉到任务内部每次调用前（见 §3.3），主线程只负责在超限/失败时不再提交新任务。

### 2.4 事件流

- 现有 SSE 事件按"角色→病例→轮次"线性推。并行后事件天然乱序。
- 方案：事件仍全量发送，但每个事件带 `case_id + role + scope` 标签；前端进度改按病例维度聚合（病例维度进度条/完成数），总进度 = 已完成任务数 / 总任务数。SSE 协议字段向后兼容（新增字段不破坏旧字段），**前端对未知事件类型默认忽略**（见 §四 前端改动）。
- ⚠️ 旧字段 `completed_calls` 并发下可能倒退（计数读取与事件发送不在同一临界区），**不再保证单调**；前端不得复用 `completed_calls` 做进度驱动，进度一律按病例完成数计算。

## 三、三大约束的解决方案

### 3.1 DeepSeek 限流（429）退避重试

- `deepseek.py` 增加重试包装（覆盖 `complete` 与 `stream` 两侧）：429 时优先尊重服务端 `Retry-After` 响应头；无该头时指数退避（1s→2s→4s→8s，上限 4 次）带抖动；重试期间发 `call_retry` 事件。
- **重试计入 `call_count` 与预算**：每发出一次真实 HTTP 请求即计数（含重试），预算公式预留重试余量（§3.3）；重试耗尽仍 429 才按失败关闭处理。
- **防雪崩**：并发任务统一从线程池调度，初始提交即按并发度均摊（`MEDGATE_LIVE_CONCURRENCY` 控制 max_workers 即天然限流）；不允许各任务无限重试。
- 其他错误（鉴权 401/403、5xx、超时）保持现有失败关闭语义，不重试。
- 重试只覆盖"单次调用"，不覆盖整个病例——病例内某 turn 重试后仍继续本病例后续轮次。

### 3.2 幂等/失败语义

- **保持整体 run 幂等不变**：幂等键仍绑定整个 run；重放（Idempotent-Replay）在预检阶段拦截，不重复调用。
- **失败语义决策（二选一，建议 A）**：
  - A（推荐）：**失败即整体失败**——任一病例任务抛未恢复异常，取消线程池剩余任务，整个 run 标记失败、不落库。理由：与现有"失败关闭"语义一致（`live.py` 现为整体失败），重放从零开始；实现简单、语义清晰，符合"门禁不可部分放行"的产品调性。
  - B（部分成功重试）：按病例粒度记录成功/失败，失败病例下次重放时跳过已成功病例。节省费用但破坏"一次 run 一份完整报告"的不变量，且前端/报告需区分部分覆盖状态。**本期不做**，留作后续增强。
- 取消与异常传播时序（关键实现约束）：
  - `ThreadPoolExecutor.shutdown(cancel_futures=True)` **只能取消未启动的排队任务**；in-flight 任务会继续发完其内部剩余的模型调用，因此"运行中超过预算即立即停止调用"在池层面**无法保证**——预算与取消检查必须下沉到任务内部每次调用前（原子检查 `call_count ≥ budget` 或 cancel 标志后协作放弃，见 §3.3）。
  - 失败路径：`record_live` 捕获异常后先 `executor.shutdown(wait=True, cancel_futures=True)` 排空在飞任务，再向 API 层传播异常；**client 生命周期归池排空之后管理**，禁止在池内任务未完成时 `close()`（`api.py:770-774` 的 finally 无条件 close 需调整为等待池排空后执行）。
  - 取消优先级：`LiveRunCancelled`（浏览器断开）优先于其他异常，保证"断开 → status=cancelled"可靠（`api.py:764-766`）。

### 3.3 预算控制（live 路径为全新实现，不是沿用 09_ 方案）

- ⚠️ 09_ 方案的 token 硬预算属于 `medgate/agent.py` 的离线 Agent 打包路径，与 live 路径无实现关联；live 当前**没有预算检查**（`api.py` 无 budget 逻辑），需全新实现。
- **预算公式（按单病例-版本实测调用链）**：每个 case-version 调用数 = `turn 数 + 1（agent 末轮）+ 1（judge）+ 复审（条件触发，`live.py:567-571`，不可固定计入）`。总额 = Σ_cases (turns+1+1+复审概率×1) × 2 版本，另加 429 重试余量（见 §3.1）。预算为**封顶上限**而非精确值，用于防失控，不用于精确计费。
- **检查时机**：提交前按公式预估并拒绝超预算请求；运行中每次模型调用前（Agent/Judge/复审统一入口）原子检查 `call_count ≥ budget` 或 cancel 标志，命中即本任务协作放弃并触发整体失败。预算检查不在池层做（池层只能停止新任务，拦不住 in-flight 任务的后续调用）。
- `external_call_count` 按"实际发出的调用次数（含重试成功）"计数，重试失败不计。

### 3.4 并发=1 回退等价性（先决条件）

- 承诺：`MEDGATE_LIVE_CONCURRENCY=1` 时行为与现有串行**完全一致**（事件序、fake.calls 顺序、fixture 顺序、报告快照逐字节一致）。
- 实现依赖：任务提交顺序固定为 `for role: for case`（§2.3）+ 聚合按 (role, case) 重排 + 线程池单 worker。三者缺一，N=1 回退不成立。
- 验收：N=1 与改造前串行跑同一 Fake 的产出哈希一致。

## 四、代码改动点清单

| 文件 | 改动 |
| --- | --- |
| `medgate/deepseek.py` | 增加 429 重试包装（complete+stream 两侧，优先 Retry-After，指数退避带抖动，可注入开关；测试用 Fake 不受影响）；超时/鉴权/5xx 行为不变 |
| `medgate/live.py` | `record_live` 改为按固定顺序（role 外、case 内）提交任务到 `ThreadPoolExecutor`；把现 for 循环体提取为 `_record_case` 独立函数；线程内每次模型调用前做预算/cancel 原子检查；`call_count` 原子化；异常路径先 `shutdown(wait=True, cancel_futures=True)` 排空池再传播；事件带 `case_id/role/scope` 标签 |
| `medgate/api.py` | worker 线程不变（仍单 run 互斥）；并发参数从 `MEDGATE_LIVE_CONCURRENCY` 读取并传入 `record_live`；client 生命周期调整：等待 record_live 排空线程池后再 close（`_execute_live_run` finally 不再无条件 close） |
| `prototype/index.html` | 进度展示改按病例维度聚合；运行弹窗显示并发数；**忽略未知 SSE 事件类型（默认跳过）**；不依赖 `completed_calls` 单调性 |
| `tests/` | 新增：并发执行正确性（用**顺序无关 Fake**：response_id 按 case+role+turn 确定性生成、调用记录用线程安全容器、断言用集合/计数）、429 重试（含 Retry-After）、并发预算超限协作放弃、并发=1 与串行逐字节一致、SSE 事件聚合 |

## 五、验收标准

1. 同一输入、同一**顺序无关 Fake** 客户端下，并发执行（N≥2）与串行执行（N=1）对每个 (case, role) 的 fixture 内容**逐项等价**（剥离时间戳/UUID/response_id 后结构一致）；`MEDGATE_LIVE_CONCURRENCY=1` 与改造前串行产出**逐字节一致**（含报告快照哈希）。注：报告快照含时间戳与随机 id（`engine.py:450/580`、`live.py:429`），跨 run 快照哈希必然不同，不做跨 run 哈希相等断言。
2. 150 条 × 2 版本、8 路并发、真实 DeepSeek：墙钟 ≤ 15 分钟（目标 5–10 分钟）；429 重试耗尽后失败有明确报错与可容忍阈值（定义"重试 n 次后最终失败"的成本上限，见 §六 风险 2）。
3. 任一病例任务失败或预算超限 → 整体 run 失败、无部分落库、幂等重放仍从零开始且行为一致；in-flight 任务排空后 client 才关闭。
4. SSE 前端进度按病例聚合正确，病例完成数实时增长，未知事件类型被前端忽略，无事件丢失。
5. 全量测试通过，离线/CLI 路径不回归（`run_offline` 不受影响）。

## 六、风险与回退

- **风险 1：并发下 DeepSeek 返回波动导致病例间结果不一致**（模型非确定性）。缓解：与串行同样的失败关闭语义兜底；不改变"每次 run 独立"的产品语义。
- **风险 2：429 退避在真实限流下放大墙钟**。缓解：并发度可配置、重试上限封顶（4 次）、超预算失败关闭，不无限等待；定义可容忍阈值——单次调用重试 4 次后仍 429 即该任务失败并整体失败，重试总成本上限 = 并发度 × 最大退避和，纳入预算余量。
- **风险 3：前端进度聚合回归**。缓解：SSE 字段向后兼容，前端忽略未知事件；保留串行模式开关（`MEDGATE_LIVE_CONCURRENCY=1`）作为回退。
- **回退方案**：并发度环境变量设 1 即完全回退到现有串行行为（含事件序与报告哈希），无需代码回滚。

## 七、非目标（本期不做）

- 不做 case 内多轮 turns 并行（依赖约束）。
- 不做部分成功/断点续跑（失败语义决策 B）。
- 不改调用次数与费用（并行只省墙钟时间）。
- 不改离线 `run_offline` 与 CLI（保持资产回放路径不变）。

## 八、定案记录（2026-08-17 实施确认）

1. 失败语义 A（整体失败）**已采纳**——并发下任一病例失败或预算超限即整体失败、不部分落库。
2. 默认并发度 **4**（`MEDGATE_LIVE_CONCURRENCY` 可配，上限 16；设 1 即回退串行）。
3. 429 重试：`max_retries=4`、优先 Retry-After、指数退避带抖动，`on_retry` 回调可注入（当前仅计数，不注入 SSE 事件；重试有预算余量兜底）。
4. `external_call_count` 口径 = 成功发出的调用链次数（每 case-version 的 agent turns + judge + 条件复审），不精确含重试次数；重试活动不改变该计数。