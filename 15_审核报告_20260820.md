# MedGate 全量审核报告（2026-08-20）

| 项目 | 内容 |
| --- | --- |
| 审核对象 | 本仓库全部现行代码、资产与文档：`medgate/` 包（11 模块）、`tests/`（134 项）、`assets/`（3 测试集 + 3 fixture + manifest）、`scripts/`（4 个 node 脚本）、`prototype/`、`.github/workflows/ci.yml`、README / AGENTS / PROGRESS / BLOCKED / 00_~14_ 文档、NOTICE |
| 审核基线 | `main @ 8dfec62`，工作区 clean，`origin/main` 已同步；远端 CI run 32357265849（对应本 HEAD）三 job 全绿 |
| 审核视角 | 求职作品集 + 对外公开仓库（招聘方 5 分钟浏览 + clone 自跑）× 医疗门禁语义正确性 |
| 审核日期 | 2026-08-20 |
| 审查模式 | 双只读对抗审查 Agent（正确性攻击者 / 简化与证据攻击者，无降级）+ 主 Agent 亲跑实证并复核全部 P0 与关键 P1 断言 |
| 处置 | 只出报告，未修改任何项目文件 |

---

## 一、总评

**代码与门禁语义：8/10，可交付。** 核心不变量（候选未关闭 P0 → BLOCKED、失败关闭、幂等重放、三态退出码、密钥不入库）经主路径实测与对抗审查均成立，134 项测试未见同义反复，质量显著高于演示项目平均水平。但三个边界场景（人工降级、异常退出码、multidim 全 live-only 集合）使"零容忍"声明打折，属 P1。

**文档与证据可信度：6.5/10，需一轮口径收口。** 诚实披露文化高于平均（未覆盖项坦白、带日期回填），但存在 1 处虚假技术栈声明（P0）与约 10 处同日快速迭代留下的口径漂移；live 维度的核心叙事证据全部滞留本机，clone 后不可见。

**结论：有条件通过。** 离线门禁主链路可信、CI 实跑全绿、作品集骨架成立；修复下文 P0-1 与 P1 文档漂移群后，对外可信度才能兑现其自称的证据标准。P1 代码类三项不阻塞当前演示路径，但应在 P2 大规模真实运行前修复（届时降级与异常路径会被真实触发）。

## 二、主 Agent 亲跑实证（全部通过）

| 验证项 | 命令/方式 | 结果 |
| --- | --- | --- |
| 全量单测 | `python -B -W error::ResourceWarning -m unittest discover -s tests` | 134/134 通过（1.1s） |
| 资产校验 | `python -m medgate validate` | status=ok，fixture_count=24，expected_gate=BLOCKED |
| 离线回放三态 | `python -m medgate run`（无 Key） | 真实退出码**恰好 1**，gate.state=BLOCKED，UNRESOLVED_P0，external_call_count=0 |
| 幂等重放 | 同 `--idempotency-key` 连跑两次 | 两次退出码一致（1），结论一致 |
| node 静态四脚本 | `validate_assets / smoke_assets / check_prototype / smoke_prototype` | 均 status=ok / exit 0 |
| 远端 CI | GitHub API 查询 | 最近 3 run（8dfec62 / e28e895 / a098e89）全部 completed **success**；最新 run 对应当前 HEAD |
| CI 断言写法 | 逐行读 `ci.yml` | 三态断言按"恰好等于 1"而非"非零即失败"，含 external_call_count==0 无外连证据、报告七条内容断言 |

## 三、问题清单

### P0（1 项）——对外公开造成误导

**P0-1｜虚假技术栈声明：05_ D-09 写 React + TypeScript + shadcn/ui，实际原型是纯 vanilla 单文件**
- 证据：`05_关键决策记录.md` D-09「我的选择：React + TypeScript + shadcn/ui」，验证字段写完成式「键盘可完成主链路；4 个页面视觉一致」；`prototype/index.html` 中 `grep -i react|typescript` **0 处命中**，实为纯 HTML/CSS/JS 单文件（这也是 08-12 审核时确认过的形态）。
- 影响：05_ 是 README「继续阅读」推荐文档。招聘方据 D-09 误信技术栈后点开原型即穿帮，直接损伤「诚实披露」这一作品集核心卖点；头部免责声明只覆盖「验证字段」，未覆盖该已放弃选型。
- 改法：D-09 改为事实（「原型采用零依赖单文件 HTML/CSS/JS；曾评估 React + shadcn/ui，为保持 clone 即开即用而弃用」），保留弃用理由反而是加分项。

### P1（8 项，按类分组）

**代码语义类（均为对抗审查实测复现、主 Agent 逐行复核确认）：**

- **P1-1｜P0 人工降级在两条路径语义相反。** `record_review(confirmed, effective_severity="P1")` 后：`recalculate_gate` 路径在 `engine.py:884-885` 将 finding 内存 severity 更新为降级值、`:911-914` 据此判定 → BLOCKED 变 REVIEW_REQUIRED；run 内 ReviewPack 路径在 `engine.py:596-623` 只把降级写库（`UPDATE findings SET severity`），不更新参与门禁计算的内存 `finding["severity"]`（仅 `:609` 写 review 字典）→ 仍 BLOCKED。同一 run 两种门禁结果，「未关闭 P0 → BLOCKED」的「关闭」定义不一致。建议以 recalculate 语义为准统一（人工确认降级有效），ReviewPack 路径同步更新内存 severity，并补 gate 级断言测试。
- **P1-2｜未捕获异常以退出码 1 泄漏，冒充 BLOCKED。** `cli.py:78` 的 except 元组不含 `sqlite3.Error`；实测伪造 `sqlite3.IntegrityError`（API 并发同 key 时 db UNIQUE 兜底真实可触发）异常直接逃逸，Python 未捕获异常默认退出码 1 = BLOCKED 语义，违反「执行错误=3」约定。改法：元组补 `sqlite3.Error`，并加兜底 `except Exception → exit 3`。
- **P1-3｜multidim 边界层 21/22 case 无任何执行路径，整集仍恒 PASSED。** 主 Agent 亲跑 `medgate run --test-set multidim-v1` 退出码 0（PASSED）——82 例中仅 4 个关键 case 有 fixture，其余（含 21 个零容忍 boundary case）`not_evaluated` 不计 Gate（`multidim.py:173-181`）；且 live API 只支持 pretriage（`api.py:654` `load_bundle` 不接收其他 testset），boundary 的 `candidate_answers` 参数无生产调用方。「边界层零容忍」（D-12）实际仅覆盖 1/22。BLOCKED.md 已如实披露「live 验收待用户 Key」，属未完成而非错误，但「无 case 被评 → PASSED」违背「未评估≠通过」原则：整集全部 not_evaluated 时应输出显著区分的状态（如报告标注 coverage，或 boundary 层有未评估 case 时拒绝输出 PASSED）。
- **P1-4｜配套测试缺口。** 无测试覆盖 confirmed+降级对最终 gate 的影响（现有 review 测试只测 false_positive），P1-1 因此不可见；`test_live.py:178` 复审 pass 翻转 case-003（P0）仅断言单 case verdict，未断言最终 gate=REVIEW_REQUIRED。

**文档口径类（同日三段快迭代留下的漂移群）：**

- **P1-5｜四处活文档口径过期。** ①`AGENTS.md:56`「当前 110 项」，实际 134（README 已正确）；②`14_开发计划 §〇` 执行顺序表仍标「B ← 当前」，实际 B 段与 C 段 P1-1~P1-4 均已完成，落后两阶段；③`12_` §四落地方案仍写「NOHARM 案例改写（CC-BY-4.0）」，与同文件 §六「不适用、撤下」自相矛盾（§四漏同步）；④`05_:3` 头部「真实 DeepSeek 仍待实跑」与 `14_:34`「已完成 15 次真实 live run」矛盾。另 `00_项目说明.md` 状态表停在 08-20 上午、未提 P1-1~P1-4，且「4 个页面」应为五页面。
- **P1-6｜条目级边界声明与许可证载体缺失。** 「未经执业医师复核」在 3 个 testset JSON、3 个 fixture、examples 回归集中 0 处（AGENTS.md 红线要求「所有合成与改写条目保留该口径」未落到数据文件层，文件单独流转时声明丢失）；`complex-v1.json` 本体无 license/来源元数据（仅 manifest 与 NOTICE 有）；仓库根**无 LICENSE 文件**（自身许可未声明）；NOTICE 未附 Apache-2.0 许可证副本（其 §4 的要求）。建议：testset/fixture 顶层加 `disclaimer` 与 `license_ref` 字段、校验脚本断言、根目录补 LICENSE、NOTICE 附 Apache-2.0 文本链接或副本。
- **P1-7｜01_/02_ 本体无「已裁剪、当历史读」标注。** 该警示只在 README/AGENTS；文件名序号排最前，GitHub 直达读者大概率先读到这两份已判「范围超载」的首版方案。文件头部加一段两行警示即可。
- **P1-8｜live 维度证据 clone 后不可见，状态表未分级。** 「15 次真实 live run」「V1→V3 真实重跑 12/12」「规则层捕获 Judge 幻觉（case-002）」是作品集最有说服力的叙事，但产物全在本机 `artifacts/`（.gitignore 排除，14_:154 自认「干净 clone 拿不到 case-002」）；README 状态表把「Fake Client 验收」的 M3.1 与「真实重跑验收」的 V3 混排同级 ✅。建议：挑 1 份脱敏 live 报告快照入库 `examples/`，状态表对每行加「离线可复现 / 本机已验 / 待真实冒烟」三级标注。

### P2（13 项，列出备查）

代码类：①`engine.py:320` case priority 非精确 "P0" 时 fail severity 回落 judge severity，`assets.py:159-211` 不校验 priority 枚举（资产变更时静默降级隐患）；②`live.py:305-357` `_parse_review` 不校验 final_verdict 与 verification 表一致性（LLM 幻觉可产生自相矛盾裁决，有 REVIEW_REQUIRED 兜底）；③`engine.py:131-132`「您可能是肺炎」因「您…是」被计为 definitive_medical_assertion，「可能」未豁免（仅报告级）；④`cli.py:54` `gate` 子命令完全信任报告 JSON 的 exit_code（伪造 exit_code=0 可骗过 CI 断言链的该环节）；⑤`api.py:735-745` 持 live_lock 贯穿分钟级 record_live，LIVE_RUN_BUSY 分支基本不可达；⑥`live.py:630-634` 预算计数不含 deepseek 内部 429 重试，external_call_count 低估；⑦`prompts.py:140-151` bad_case_count 按 prompt hash OR 匹配，版本作为 baseline 时 candidate findings 混入；⑧`engine.py:485` request_hash 为 NULL 的旧记录跳过冲突校验直接重放；`db.py` 无 WAL/busy_timeout。

文档类：⑨`BLOCKED.md:12` 标题「P1-1/P1-2 已建」漏 P1-3/P1-4；⑩`AGENTS.md:11`「18 份 markdown」实为 19；⑪`05_` D-02 提及的 `gen_fixture.py`/`validate_cases.py` 不在仓库；⑫README 快速开始未提示 `--test-set multidim-v1/complex-v1`；⑬CI 只测 Python 3.12，`pyproject` 声明 `>=3.11` 未实测（PROGRESS 已自报）。

## 四、证据强度分层（对招聘方的诚实披露建议）

- **外部可复现（最强）**：离线门禁主链路（clone + 三条命令产出 BLOCKED）、134 项单测、CI 徽章与 Actions 历史、node 静态校验。
- **本机已验、clone 不可见**：15 次 live run、V1→V3 真实重跑、case-002 Judge 幻觉捕获、并发+429 行为、M3.1 v2 Fake Client 验收。
- **尚未做**：FAQ/复杂/边界层的真实评分分布（P1-6 live 冒烟，需用户 Key）、真实 v2 smoke、P3-3② 引用蕴含、M3.2/M3.3、执业医师复核。

## 五、建议修复顺序与复验条件

1. **立即（分钟级）**：P0-1 改 D-09 为事实；P1-5 四处口径同步（AGENTS/14_§〇/12_§四/05_ 头部/00_）。
2. **P1-6/P1-7/P1-8（小时级）**：LICENSE + 数据文件级 disclaimer/license_ref + 01_/02_ 头部警示 + 一份脱敏 live 报告入库。
3. **P2 大规模真实运行前（P1 代码类）**：P1-1 统一降级语义 + gate 级测试；P1-2 补 except；P1-3 not_evaluated 覆盖状态；P1-4 补测试。CI 全绿 + pretriage 零回归是硬约束。
4. P2 项随手修，不单独立项。

**复验条件**：P0-1 与 P1-5 修复后本轮审核闭环；P1 代码类修复需附新测试（P1-1/P1-4）且 134+ 单测全绿、pretriage 离线回放退出码仍恰好 1、CI 三 job 全绿。

## 六、审查模式披露与未覆盖项

- 触发依据：正式审核（用户指令）+ 跨模块复杂度。审查角色：2 个独立只读强模型审查 Agent（正确性攻击 / 简化与证据攻击），无降级；主 Agent 亲跑 7 项实证并逐行复核 P0-1、P1-1、P1-2、P1-3 后才定级。P0 1 项 / P1 8 项 / P2 13 项，两路去重后有效新增 0（无重复报告）。审查成本相对预期返工收益：**高**（1 项诚信级 P0 与 3 项实测复现的门禁边界缺陷）。
- 未覆盖：真实 DeepSeek live 路径（未外发 Key，按红线不跑）；multidim/complex 120 条的医学内容正确性（无执业医师复核）；Python 3.11 兼容性；`09_`（45K）全文只抽查未逐行；前端交互仅静态断言 + 文档走查，未做真实浏览器回归（本机 headless 曾 SIGABRT，沿用 08-12 口径不冒充已验）。
