# MedGate 需求方案 v0.1 + 技术方案 v0.1｜审核意见

| 项目 | 内容 |
| --- | --- |
| 审核对象 | [`01_需求方案.md`](./01_需求方案.md)（515 行）、[`02_技术方案.md`](./02_技术方案.md)（623 行） |
| 审核基线 | 两份文档 2026-08-12 版本，全文逐节读取 |
| 审核视角 | 求职作品集（招聘方要求：本人 coding、可公开访问的产品链接或 Demo） |
| 审核日期 | 2026-08-12 |
| 审查模式 | 主 Agent 单人自审（Agent 工具在本会话默认禁用，未启动多 Agent 对抗审查，属降级） |
| 处置 | 只出意见，未修改原文 |

---

## 一、总评

**文档质量很高，但它在解决错误的问题。** 作为一份工程设计文档，这两份稿子的严谨度超过大多数在职团队的正式 PRD——不可变版本、attempt supersede、门禁 input_hash、许可证 allowlist，这些都是真本事。问题在于招聘方要的是**一个能点开的链接**，而这两份文档描述的是一个 3–5 人团队做 2–3 个月的系统；一个人按这个范围推进，最可能的结局是卡在 T1/T2，最后交出 65KB 文档和 0 个可访问页面。

**最大的单一问题**：范围严重超载。次大的问题是"门禁"这个核心卖点在机制上有两个未闭合的洞（门禁归属对象未定义、不接 CI），面试时会被工程背景的面试官当场问穿。

**成熟度判断**：作为设计文档 8 成；作为面试交付物 1 成（因为零可运行产物）。

---

## 二、优点（真优点，不是客套）

1. **P0 硬门禁与加权总分解耦，并且在数据结构上落实了**。不是喊口号——`gate_decisions` 只读 active attempt 下未 supersede 的结构化事实，带 `input_hash`，优先级 `BLOCKED > REVIEW_REQUIRED > PASSED`，明确"不提供一键强制放行"（需求 356 行）。这是整个方案最硬的产品判断，在医疗场景成立且可辩护。

2. **把"无密钥回放"定义成评测引擎的一等执行模式，而不是 mock 页面**（技术 37 行）。这一刀直接解掉了"作品集 Demo 需要密钥所以没法给面试官点"的死结，而且给了可验证的验收口径：`--network none` 跑 E2E、`external_call_count=0`、任何非数据库 egress 判失败（技术 307/519 行）。这个设计我认可，建议在面试里当成一个独立的技术判断来讲。

3. **开源许可证边界处理得极其克制**。11.3 逐数据集登记 `dataset_license`/`source_url`、构建时 allowlist 校验、未登记资产使构建失败、"代码 MIT 不代表数据集自动可再分发"（技术 151 行）。医疗 AI 领域这是真实雷区，多数候选人会直接把 benchmark 数据打包进仓库。这一节可以在面试里讲成一个完整的风险故事。

4. **历史报告不会被后来的复核改写**（技术 12.3）。报告快照绑定确定的 gate decision 而非"当前最新状态"——"可复现"这个承诺落到了机制上，不是形容词。

5. **全程没有把假设写成成果**。"性能数字是 MVP 验收目标，不是已验证的线上表现"（需求 440 行）、"真实试用前不预设改善百分比"（需求 459 行）、"Demo 结果只证明产品流程可运行，不证明模型达到临床使用标准"（需求 392 行）。这条底线守住了，面试时不会翻车。

---

## 三、问题清单

### P0｜不改就交付不了，或面试当场被问穿

---

#### P0-1 范围超载：按现文档做，交不出可访问链接

**位置**：技术方案 §15 实施计划（556–568）、§6.1 数据模型（185–201）、需求方案 §7 FR-01～FR-10、§12.2 表单边界

**问题**：14 张表、2 套状态机、attempt supersede 语义、乐观锁、幂等键、租约恢复、审计事件、SQLite+PostgreSQL 双库一致性测试、Golden 测试集、7 个实施阶段（T0–T6）、8 个页面、字段级校验规则（零宽字符/去首尾空白/2–40 字）。技术方案自己写了"最快可演示路径为 T0→T3"（568 行），但 T0→T3 里仍然包含 FR-01/FR-02 的完整 CRUD + JSONL 导入 UI + 边界校验，这已经是 4–6 周全职工作量，而这部分**面试官一秒都不会看**——没有人会因为你的"版本名称去首尾空白校验"给 offer。

**怎么改**：按"面试官会点开哪些页面"倒推，把 MVP 砍成**门禁演示**而不是**评测平台**。具体裁剪清单：

| 模块 | 现方案 | 建议首发 | 理由 |
| --- | --- | --- | --- |
| FR-01 Agent 版本 CRUD | 完整表单 + 列表 + 复制 + 归档 | **砍成只读列表**，版本由 `seed` 脚本从 YAML 装载 | 面试官看的是对比结果，不是你怎么建版本 |
| FR-02 测试集/病例 CRUD + JSONL 导入 UI | 完整表单 + 原子导入 + 行号报错 | **砍成只读**，病例存仓库 JSONL + 一个 `validate` CLI | 导入 UI 是工作量黑洞，零展示价值 |
| FR-03 任务创建表单 | 9 个字段 + 成本预估 + 二次确认 | 砍到 3 个字段（测试集版本、基线、候选） | 并发/超时在回放模式没有意义，见 P1-2 |
| FR-10 审计页 | 独立页面 + 7 列表格 | **砍掉页面**，只保留 `audit_events` 写入，在报告里带一段 | 审计的价值在"有记录"，不在"有页面" |
| 总览趋势 | 近 10 次通过率 + 维度趋势 | 砍成 4 个数字卡片 | 只有一次运行时趋势图是空的 |
| 乐观锁 / 租约恢复 / 双库一致性测试 | 全做 | **全部后置** | 单 Worker 单用户本地跑，这些防的是不存在的并发 |
| Playwright / Docker Compose | M4 后置 | 保持后置，**别提前** | 已正确后置，不要在压力下反悔 |
| 页面数 | 8 个 | **4 个**（评测详情、病例详情、发布门禁、总览） | 少 4 个页面 ≈ 省 2 周 |

保留不动的核心链路：**双版本回放 → 三层评分 → 病例矩阵 → 双栏对话+证据定位 → P0 人工复核 → gate 重算 → 报告快照**。这条链路是全部卖点，也是面试演示的全部内容。

裁剪后 T0→T3 约 2–3 周，可交付。裁掉的部分不是删除，是写进"下一步规划"——**面试官问"为什么没做 X"，答"我按可演示价值排了序，X 在 backlog"是加分项；答"还在做"是减分项**。

---

#### P0-2 门禁到底挂在哪个对象上？两份文档在打架

**位置**：技术方案 §6.3 Finding 状态机（237 行）、§12.3（475 行）、§9.2（408 行）；需求方案 §FR-09（356 行）、§12.1 验收 9（473 行）

**问题**：这是核心机制的逻辑漏洞，且正好在这个作品的招牌功能上。

- 技术 237 行：`fixed_pending_regression → regression_passed` "只能由 Worker 在**该 run** 的同一 Case ID + checkpoint 通过时写入"。
- 但 `findings` 表带 `run_id`（技术 197 行），Finding 是 run 内对象；回归是**新 run**，新 run 会产生**新的 Finding**，不会回头改旧 run 的 Finding。
- 技术 12.3：报告快照绑定确定的 gate decision，"不从'当前最新状态'动态生成"。
- 需求 12.1 验收 9："P0 进入'已修复待回归'后仍为 `BLOCKED`，直到关联新任务对同一检查点回归通过。"

把这四条放一起就矛盾了：如果旧 run 的门禁是不可变历史快照，那它**永远** BLOCKED，"直到回归通过"这句就没有落点；如果它会因新 run 而变绿，那 12.3 的不可变承诺就破了，而且语义荒谬——发布的是新版本，凭什么让旧候选版的报告转绿。

结果是 `fixed_pending_regression → regression_passed` 这条状态转换**在门禁计算上完全空转**，但需求方案把它写成了阻断逻辑的一部分。

**怎么改**：显式把两个概念切开，在两份文档各加一段：

> **门禁结论绑定一次 run，是历史事实，只在同一 run 内的人工复核动作触发重算；跨 run 的回归通过只改 Finding 生命周期状态，不改任何历史 run 的 gate decision。**
> 一个候选版本"能不能发"由针对它自己的那次 run 的 gate decision 回答；"这个历史问题修好了没"由 Finding 状态回答，展示在总览的"未处理 P0"计数里。

同步改需求 12.1 验收 9 的措辞为：

> 9. P0 进入"已修复待回归"后，其所属 run 的门禁结论保持 `BLOCKED` 不变（历史事实不可翻转）；该 Finding 在总览"未处理 P0"中持续计数，直到关联新任务对同一 Case ID + checkpoint 回归通过。

这个修改还有个副作用好处：**面试时这是一个可以主动讲的取舍**——"我一开始把门禁挂在 run 上，发现历史报告会被后来的复核翻转，所以把'发布决策'和'问题闭环'拆成了两个对象。"这比讲功能有价值得多。

---

#### P0-3 Demo 的招牌路径走不通：三处规则互相锁死

**位置**：需求方案 §2.3 场景 C（65–66）、§2.2 权限表（53）、§11.1（444–449）、§12.1 验收 6（470）；技术方案 §9.1 reports 行（398）、§14.2（540）

**问题**：整个作品集的演示脚本是"访客无密钥跑一次评测，看到 BLOCKED 报告"。这条路上有三道锁：

1. **报告只能在 `completed` 生成**（技术 398 行），但 `needs_review → completed` 要求"必审项已处理"（技术 224 行）。P0 待复核就是必审项。所以有未解决 P0 的 run 进不了 completed，**出不了报告**——而 BLOCKED 报告正是要演示的东西。技术 398 行备注写了"可展示阻断报告"，这句和"仅 completed"直接打架。
2. **"必审项已处理"从未定义**：是"有一条复核记录（无论结论）"，还是"问题已关闭"？取前者链路通，取后者链路死。两份文档都没写。
3. **访客权限自相矛盾**：需求 2.2 说访客"只读公开快照"，场景 C 说访客"完成一次评测"，技术 14.2 说"演示写操作放在受保护工作区或**每次会话隔离的临时空间**"。而需求 3.2（87 行）明确"不实现复杂租户"——"每次会话隔离的临时空间"就是轻量多租户，成本不低。

**怎么改**（三条一起改，否则改不通）：

- 在需求 §4 关键术语加一条：**必审项已处理 = 该 Finding 至少存在一条有效人工复核记录（`确认问题` 或 `误报`），与复核结论无关。**这样 P0 被"确认问题"后 run 可以 completed，gate 仍是 BLOCKED，报告可出。
- 技术 9.1 reports 行备注改为："仅 `completed`；BLOCKED 结论同样出报告，报告以门禁结论为准而非以是否通过为准。"
- 访客路径二选一，**推荐选 A**：
  - **A（省事）**：访客全程只读，看的是**预置好的一次完整运行**（含 BLOCKED 报告、P0 证据、复核记录）。演示"过程"用一段 30 秒录屏或分步截图。写操作只在你本人登录的受保护账号下演示。首发就这么做，需求 2.3 场景 C 改写为"访客浏览一次已完成运行的完整证据链"。
  - B（费事）：真做会话隔离沙箱。与 3.2"不实现复杂租户"冲突，且每日重置数据（技术 542 行）的工程量不小。**首发不要选 B。**

---

#### P0-4 30 例病例 + 60 组 fixture 的内容工作量：零预算、零工具、零复核人

**位置**：需求方案 §8（381–392）、§FR-01 回放数据包（153）、§11.1（448）；技术方案 §14.1（534）

**问题**：这是整个项目最可能烂尾的地方，而文档用一张 6 行表就打发了。实际工作量：

- **30 例病例**：每例含患者设定（20–2000 字）、首句、3–8 轮多轮脚本、至少 1 条（实际 3–6 条）检查点（每条 6 个子字段）、禁止行为、预期处置。认真写一例 30–60 分钟，30 例 = **15–30 小时**。
- **60 组 fixture**：基线版 + 候选版各 30 例的完整对话输出 + 每例的预置 Judge 结构化证据，且要**精心设计至少 1 个可解释的 P0 回退**（需求 448 行）。模型生成后必须逐条人工校对，否则演示时经不起追问。**再加 10–20 小时**。
- 合计 **25–50 小时纯内容工作**，不能自动化，不好玩，还需要医疗判断。文档里对应的支持只有一句 `make seed-demo`（技术 534 行）。
- 需求 392 行还写了"正式标注需由具备医疗背景的复核者确认"——**谁做？** 作品集里没有这个人。这句现在是空头支票，面试官追问会尴尬。

**怎么改**：

1. **首发砍到 12 例**（急症 4、特殊人群 2、信息完整 2、角色边界 2、多轮一致 2；其中关键病例 4、P0 回退 1）。全链路跑通、报告能讲之后再补到 30。文档里把 30 例改成"首发 12 例，目标 30 例"，并说明这是有意的分批。
2. **给自己造工具**：写一个 `scripts/gen_fixture.py`，输入病例 JSONL + 一个"要不要注入 P0 回退"的开关，调模型生成基线/候选双版本对话，产出符合 schema 的 fixture，人工只做校对。这个脚本本身就是"本人 coding"的证据，且能进 README 当亮点。
3. **把"医疗复核者"这句改诚实**：改成"病例与标注由本人编写，未经执业医师复核；这是 Demo 的已知限制，真实使用前需补医疗复核环节"。**主动承认限制远好过被问出来。**
4. 在实施计划表里给内容工作**单列一行 T2.5「病例与 fixture 生产」**，标明预估工时。现在它藏在 T1/T2 里，会被低估。

---

#### P0-5 叫"发布门禁"却不接 CI，门禁就不成立

**位置**：需求方案 §1.2 痛点 4（27 行）、§FR-09；技术方案 §9 API 设计（全节）

**问题**：需求 1.2 把"评测报告与上线流程脱节，即使发现 P0 也可能继续发布"列为第四个痛点——这恰恰是**唯一必须靠 CI 集成解决**的痛点。但技术方案 §9 的 API 全部是给 Web UI 用的：没有 CLI、没有 webhook、没有 CI 状态回写、没有非 0 退出码。

**结果**：这个产品拦不住任何一次发布。它是一个报告页面，不是门禁。工程背景的面试官会在 3 分钟内问到这里。

顺带一个更狠的问题你要准备好答案：**"为什么不是一个 CI 脚本？"** 现实中评测通常就长在 GitHub Actions 里。这个产品要论证 Web 工作台的必要性——答案应该是"因为 P0 需要医疗质控人工复核，人工复核回路无法塞进 CI，所以门禁必须是**异步的、有状态的、可等人的**"。这个论证目前文档里一个字都没有，但它恰好是你最强的差异化。

**怎么改**（成本很低，收益极高）：

1. 技术 §9 加一节 **9.3 CI 集成**，给三样东西：
   - `medgate run --agent candidate-v2 --test-set 预问诊安全集@v1 --wait`，退出码：`0=PASSED`、`1=BLOCKED`、`2=REVIEW_REQUIRED`。
   - `GET /eval-runs/{id}/gate` 返回可被 CI 轮询的门禁状态。
   - 一段 20 行的 `.github/workflows/medgate-gate.yml` 示例，展示"P0 未关闭 → CI red → merge 被拦"。
2. 需求 §FR-09 加一小节"门禁如何真正拦住发布"，把上面那个"人工复核回路塞不进 CI"的论证写进去。
3. 这一节在面试里的价值 > 任何一个 CRUD 页面。**优先级排在所有 UI 之前。**

---

#### P0-6 缺竞品差异化：答不出"这和 Langfuse / Promptfoo 有什么区别"

**位置**：需求方案 §1.2（22–28）——全文无竞品章节

**问题**：需求 1.2 列的四个痛点（评测不可复现、只看平均分、缺证据链、与上线脱节）**前三个 Langfuse / Braintrust / Promptfoo / DeepEval / Ragas 都在解决**，而且做得比一个人的作品集成熟。面试官的第一个问题必然是这个，现在文档零准备。

**怎么改**：**单独建一份 `03_竞品与差异化.md`**（不要塞进需求方案），一页讲清三件事：

1. **对照表**：Langfuse / Promptfoo / Braintrust / DeepEval，各自在"可复现""证据链""人工复核""发布拦截""领域安全规则"五列上的覆盖情况。老老实实标出它们已经做到的部分。
2. **真差异只有两条，讲透就够**：
   - **医疗 P0 的定义权在人，不在模型**——通用平台的 assertion 是"分数阈值"，医疗场景是"漏掉急症升级 = 一票否决"，且这个判断必须由持证质控人复核。这决定了门禁必须是异步有状态的，而通用平台的门禁是同步的 CI assertion。
   - **门禁不可绕过 + 证据链可审计**——通用平台的通过与否可以被改阈值绕开；这里 P0 硬规则与加权分解耦，且"MVP 不提供一键强制放行"。这是合规逻辑不是产品逻辑。
3. **诚实边界**：明确写"通用可观测性、trace、成本追踪不做，这些应该用现成平台"。**主动划出不做的地界，比声称什么都做可信。**

---

#### P0-7 通篇没有"我"：这是系统文档，不是作品集

**位置**：两份文档全文

**问题**：65KB 里没有一处第一人称，没有一句"我为什么这么选"、"我否掉了什么"。技术 §2.1 架构取舍和 §18 待确认决策是最接近的，但写成了"建议…"的无主语方案语言。**招聘方看到的是一个凭空出现的完备系统，看不到一个做判断的人。**

同时缺一份**面试官会真正打开的第一入口**：现在只有 01/02 两份长文档，没有人会读 65KB。

**怎么改**：补两份文件（这两份的优先级高于继续打磨 01/02）：

1. **`00_项目说明.md`（一页，硬上限 1 页）**：一句话定位（≤40 字，见 P2-5）、为什么做这个题、Demo 链接、3 分钟演示脚本（点哪里、说什么）、我做了什么（设计+编码全栈）、已知限制。这是面试官唯一保证会读的文件。
2. **`04_关键决策记录.md`**：把技术 §2.1 和 §18 抽出来扩成 6–8 条第一人称决策，每条写「问题 → 我考虑的选项 → 我选了什么 → 代价是什么」。现成的好素材至少有 6 条：
   - 回放模式做成一等执行模式而非 mock（代价：fixture 生产成本）
   - P0 硬门禁与加权分解耦（代价：会有"分数很高但被拦"的反直觉情况）
   - 门禁绑定 run 而非版本（P0-2 修完之后，这就是一条完整的"踩坑—修正"叙事）
   - 数据库队列而非 Celery/Redis（代价：单 Worker 吞吐）
   - 不运行第三方研究仓库，只做适配器隔离（代价：放弃现成 benchmark 覆盖）
   - 首发砍到 12 例而非 30 例（代价：覆盖度，换交付确定性）

---

### P1｜会被追问，或实现时一定绊住

---

#### P1-1 M/T 双轨阶段编号从未定义，至少 3 处不一致

**位置**：技术方案 113、98、99 行；§15（556–568）；需求方案 §13（496–502）

- 技术 113 行目录树注释：`adapters/ # MVP replay；**M3 live**；后续 Medmarks` —— 全文其他地方一致写 M4，**这一处是硬错**。
- 技术 98 行：`托管数据库 PostgreSQL 16+ | **M4 后置**`，但技术 §15 把 PostgreSQL 放在 **T4**，而 T4 对应需求的 **M3 作品集发布**。标注错位。
- 技术 99 行 Playwright 同上。

**怎么改**：在技术方案 §15 顶部加一张 M↔T 映射表（M0↔T0、M1↔—、M2↔T1–T3、M3↔T4、M4↔T5–T6），然后全文只用一套编号（建议对外文档统一用 M，技术方案内部用 T 并在每处标注对应 M）。113/98/99 三处逐个改正。

#### P1-2 「最大并发 1–10」与单 Worker 架构直接冲突；回放模式下超时/参数字段是死字段

**位置**：需求 236–237 行；技术 95、281 行；需求 152–153 行、156 行、514 行

- 需求 FR-03：`最大并发 | 默认 3 | 整数 1–10`；技术 §3 是"单独 Python Worker 进程"、§7.1 是"SQLite **单 Worker**"。**要么改需求为固定 1、要么说明 Worker 内部用协程并发**——现在读者会认为可以并发 10。
- 需求 FR-03 `单轮超时 60 秒`：回放模式读本地文件，这个字段毫无意义。
- 需求 FR-01 `参数 | 默认 temperature=0`：回放模式同样无意义。
- 需求 156 行和 514 行已经说了"实时字段由功能开关隐藏、不进入首发页面"，但 FR-03 表里并发/超时**没被标为实时专属**，与之冲突。

**怎么改**：在 FR-01 和 FR-03 的字段表加一列「首发是否展示」，把适配器、模型标识、端点引用、参数、最大并发、单轮超时全部标"否（M4）"。首发的任务创建表单就剩测试集版本、基线、候选、任务名。

#### P1-3 数据模型缺 users / roles，但三处验收依赖"医疗复核权限"

**位置**：技术 §6.1（185–201）——无用户表；需求 341 行、技术 408 行、需求 12.1 验收 5

需求 341 行："P0 的误报判定或严重度下调仅允许具有'医疗复核'权限的操作者提交…不依赖前端隐藏按钮作为权限控制"；技术 408 行："服务端校验：操作者具有医疗复核权限"。但 §6.1 十四张表里**没有 users 表**，只有 `audit_events.actor_id`、`reviews.reviewer_id` 两个悬空外键。

**怎么改**：加一张最小 `users` 表（`id、display_name、role`，role 枚举 `viewer/operator/medical_reviewer`）。不需要密码体系——MVP 可以是本地配置里预置 3 个账号 + 一个简单会话。**但表必须存在**，否则 FR-08 和 12.1 验收 5 无法实现。

#### P1-4 FR-09 硬规则 1 的状态枚举不完整

**位置**：需求 351 行 vs 需求 356 行 vs 技术 359–362 行

规则 1 写"P0 为'待复核'或'确认问题'"（两态），356 行补充说三态都算未解决，技术伪代码是三态。**硬规则条目会被直接翻译成代码和测试用例，它自己必须自洽。**

**怎么改**：351 行改为"任一候选版 P0 处于「待复核」「确认问题」「已修复待回归」之一，结果为 `BLOCKED`"，356 行的补充说明保留。

#### P1-5 自建 UI 组件层是首发阶段的坏决定

**位置**：技术 91 行

`UI 基础 | 原生 CSS 变量 + 轻量组件层 | MVP 必选`，理由写的是"作品集需要可定制视觉"。但手写表格、模态、表单、Toast、骨架屏 = 1–2 周，而**面试官不看你的 CSS 架构**，只看页面清不清楚。这与"最快可演示路径 T0→T3"直接冲突。

**怎么改**：首发直接上成熟组件库（shadcn/ui 或同类），只在配色和排版上做定制。省下的时间投到 P0-4 的病例内容和 P0-5 的 CI 集成——那两处才是分差所在。

#### P1-6 访客写权限与"不实现复杂租户"冲突

见 **P0-3 第 3 点**，此处不重复。归类上它是 P0-3 的一部分。

---

### P2｜实现时的摩擦，不影响面试

| # | 位置 | 问题 | 改法 |
| --- | --- | --- | --- |
| P2-1 | 技术 196 vs 243 行 | §6.4 说"为旧自动结果写入 `superseded_at`"，但 §6.1 的 `evaluation_results` 表**没有这个字段** | 二选一。推荐删掉 6.4 里对 result 的 supersede 表述——result 归属 attempt，通过 `attempt.is_active` 即可推导，加字段是冗余 |
| P2-2 | 技术 §9.1 全表 | API 命名三种风格混用：`/clone`、`/archive`、`/publish`（路径动词）vs `:cancel`、`:revoke`（冒号）vs `/cases:import`、`/gate:calculate`（子资源+冒号） | 统一成一种。推荐非 CRUD 动作一律 `{resource}/{id}:{action}` |
| P2-3 | 技术 §6.1、需求 10.3 | 数据模型无 token / cost 字段，但需求 §6 信息架构要"成本预估"、技术 10.3 要"金额、token 使用数值字段"、12.2 指标要"token 数" | `case_attempts` 加 `prompt_tokens`、`completion_tokens`、`cost_micros`；或首发直接删掉成本相关的 UI 描述（回放模式无成本） |
| P2-4 | 需求 176 行 | `数据来源` 可选 `许可公开数据`，但 MVP 全是自编合成，3.2 又明确不再分发许可不清数据 → 首发是死选项 | 首发只留 `自编合成`，其余标 M4 |
| P2-5 | 需求 18 行 | 一句话定义 100+ 字，面试口播用不了 | 补一个 ≤40 字版本，例如："让医疗 AI 的每次版本更新，在 P0 安全问题关闭前发不出去。" 长版留在文档里 |
| P2-6 | 技术 499 行 | 集成测试要"SQLite 和 PostgreSQL 行为一致"，但 PG 要到 T4 才引入 | 移到 T4 之后。单人项目提前做双库测试性价比极低 |
| P2-7 | 技术 193、403 行 | `Idempotency-Key` 说"同操作者、同键"生效，但 `eval_runs.idempotency_key` 未说明是否与 actor 组合唯一 | 明确唯一约束为 `(actor_id, idempotency_key)` |
| P2-8 | 需求 250–251 vs 技术 213–222 | 状态机小差异：技术允许 `draft → canceled`，需求未列；`needs_review` 能否取消两边都没说 | 以技术方案为准补齐需求 FR-04 的分支 |
| P2-9 | 需求 153 行 vs 技术 §9.1 | 回放数据包 10 MB 限制在需求里有，技术 API 表 `replay-fixtures:import` 行漏了 | 技术 API 表补上"10 MB / 与测试集版本 Case ID 全覆盖" |

---

## 四、修改方案（按优先级排序的行动清单）

### 第一批：做完这批才写代码（预计 1–2 天）

1. **裁剪范围**，按 P0-1 的表改技术 §15 实施计划和需求 §13 版本规划。首发页面从 8 个砍到 4 个，病例从 30 例砍到 12 例（P0-4）。这一步是其余所有步骤的前提。
2. **补 `00_项目说明.md`**（一页，P0-7）。先写它，它会反过来逼你确认"这个 Demo 到底演示什么"。
3. **补 `03_竞品与差异化.md`**（P0-6）。面试第一问的答案。
4. **改需求 §2.3 场景 C + §2.2 权限表 + 技术 §9.1 reports 行**，按 P0-3 方案 A 打通演示路径，并在需求 §4 术语表定义"必审项已处理"。

### 第二批：写代码之前定死的机制（预计半天）

5. **改门禁归属语义**（P0-2），两份文档各加一段，改需求 12.1 验收 9。
6. **加技术 §9.3 CI 集成**（P0-5），含 CLI 退出码约定 + 一段 GitHub Actions 示例。
7. **修 P1-1 三处阶段编号错标**（技术 113/98/99），加 M↔T 映射表。
8. **修 P1-2**（字段表加"首发是否展示"列）、**P1-3**（加 users 表）、**P1-4**（FR-09 规则 1 补第三态）。
9. **P1-5 换组件库决策**，改技术 §3 第 91 行。

### 第三批：开工后随手改（不阻塞）

10. P2-1 ～ P2-9 逐条修正。
11. 边写边补 **`04_关键决策记录.md`**（P0-7），每做一个取舍就记一条，不要等到最后回忆。

### 交付顺序建议

> **CI 集成示例（P0-5）和 12 例病例内容（P0-4）要排在所有 CRUD 页面之前动手。** 前者是"门禁"这个名字的合法性证明，后者是最长的关键路径且无法压缩。UI 打磨排最后——页面丑一点不致命，链接点不开才致命。

---

## 五、没有采纳的批评方向（说明理由）

- **"技术方案对 PM 岗位来说太深了"**：不适用。招聘方明确要求本人 coding，技术深度在这里是资产不是负债。真正的问题是深度用错了地方——深在数据库并发控制上（面试官不看），浅在 CI 集成和竞品论证上（面试官必问）。
- **"性能指标 60 秒不可信"**：可信。30 例 × 2 版本 ≈ 60 个 attempt，本地 SQLite WAL + 无网络，60 秒是宽松的。且文档已声明这是验收目标非实测值（需求 440 行），处理得当。
- **"许可证章节太啰嗦"**：不改。医疗 AI 领域这是真实风险，而且是面试可讲的差异化素材。

---

## 六、本次审核的未覆盖项

- 未核对 Medmarks / CRAFT-MD 仓库的当前实际状态（许可证、目录结构、CLI 是否如技术 §5.1 所述）。技术方案 622 行已自行标注"进入编码前应锁定具体 commit 并再次核对 LICENSE"，风险已披露。
- 未评估 30 例病例的**医学内容正确性**（文档尚未产出具体病例）。
- 未做多 Agent 对抗性审查（Agent 工具在本会话默认禁用），本报告为主 Agent 单人自审结果。
