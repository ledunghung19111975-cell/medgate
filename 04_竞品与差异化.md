# MedGate｜竞品与差异化

| 项目 | 口径 |
| --- | --- |
| 主分析对象 | MedGate 医疗 AI 发布门禁 |
| 参考对象 | Promptfoo、Braintrust、Langfuse、DeepEval |
| 目标读者 | AI 产品经理面试官 / 技术面试官 |
| 证据日期 | 2026-08-12 |
| 证据范围 | 已复核官方公开文档；未完成本地安装/试跑、用户访谈或付费版本验证 |

## 一、品类背景

### 1. 产品价值

LLM 评测工具已经能够管理测试数据、自动评分、对比实验和接入 CI。真正困难的部分不再只是“有没有分数”，而是：当自动结论不稳定、领域风险不等价、关键问题需要专家确认时，团队如何形成可追踪的发布决策。

MedGate 面向医疗 Agent 团队，首个场景聚焦预问诊版本回归。它不试图替代通用评测与可观测性平台，而是验证一个更窄的问题：**如何把医疗 P0 规则、人工复核和 CI 发布状态组织成一个有证据、可等待、可追踪的闭环。**

### 2. 市场与趋势口径

本稿不引用“LLM 评测市场规模”，因为不同报告会把可观测性、Prompt 管理、红队测试和模型监控混在同一口径中，不能支撑本项目取舍。

官方产品能力能够确认的趋势是：通用平台正在同时覆盖离线实验、自动评分、人工标注和 CI/CD。Langfuse 将 Trace、Prompt、评测和标注放入一体化平台；Braintrust 把不可变实验、人工复核和 CI 串联；Promptfoo、DeepEval 直接把评测放入开发测试流程。因此，MedGate 不能把“有人审”或“能进 CI”当作独有能力，只能在领域规则和工作流默认值上建立差异。

### 3. 竞争态势

**Promptfoo 是最接近的功能对标。**它以 CLI、断言、矩阵对比和 CI quality gate 见长，最能回答“为什么不直接写一个测试脚本”。其优势是成熟、覆盖广、开发者工作流自然；相对 MedGate 设想，它不是一个针对医疗 P0 生命周期预设的质控工作台。

**Braintrust 是最强的流程参照。**其官方文档已经覆盖不可变 Experiment、人工 Review、自动评分与 CI/CD。它说明“评测 + 人工”并不是 MedGate 的独创，也迫使 MedGate 把差异收窄到医疗规则、三态门禁语义和病例级证据呈现。

**Langfuse 是平台化边界参照。**它覆盖 Trace、Prompt 管理、Dataset、Experiment、LLM Judge、代码评测和领域专家 Annotation Queue。MedGate 不应复制这些通用平台能力，而应保持窄而有立场。

**DeepEval 是框架参照。**它可通过 Python/pytest 执行评测并接入 CI，也支持 Agent、RAG 和自定义指标。相对它，MedGate 的目标不是扩充指标库，而是提供可被非工程角色理解的医疗发布证据链。

## 二、MedGate 产品分析

### 1. 核心定位

**一句话定位：让医疗 AI 的版本更新在 P0 安全问题关闭前无法通过发布检查。**

目标用户是医疗 AI 团队中的产品经理、Agent 工程师和医疗质控人员。首发不是通用 SaaS，而是一个可公开演示的、带领域规则的参考实现。

### 2. 已核实能力与计划能力

下表中，竞品能力来自官方文档；MedGate 一列全部是 **v0.2 计划能力，尚未实现**。

| 能力 | Promptfoo | Braintrust | Langfuse | DeepEval | MedGate v0.2 计划 |
| --- | --- | --- | --- | --- | --- |
| 数据集/实验回归 | 支持测试配置与结果矩阵 | 支持不可变 Experiments | 支持 Datasets 与 Experiments | 支持测试用例与评测 | 12 例固定安全集 |
| 确定性/语义评分 | 断言、阈值、模型评分 | Scorers / classifiers | Code evaluator / LLM Judge | Metrics / custom code | 规则 + 预置 Judge 证据 |
| 人工复核 | 官方 Getting Started 说明可在 Web UI 查看并人工分析结果；未确认结构化复核队列 | 支持 Human review | 支持 Annotation Queues | 官方文档确认评测结果、reason 与 CI；未确认内置人工复核队列 | P0 必审与结构化结论 |
| CI/CD | 支持 quality gate 与多类 CI | 支持在 CI/CD 跑 Experiment | 可通过 API/SDK 接入自定义 CI | 支持 pytest/CI | 三态 CLI + 示例 Action |
| 通用 Trace/Prompt 管理 | 非主定位 | 支持完整开发工作流 | 核心能力 | 非主定位 | 明确不做 |
| 医疗 P0 默认规则 | 未核实产品内置 | 未核实产品内置 | 未核实产品内置 | 未核实产品内置 | 作为首发核心资产 |
| 无密钥公开回放 | 可对既有输出做 assertion | 未核实 | 未核实 | 本地执行可不依赖平台账号 | 固定 fixture、零外连演示 |

“未确认内置队列”不等于竞品不支持，只表示本轮官方资料没有形成足以把它写成结构化人工复核产品能力的证据。Promptfoo 官方文档明确展示了 Web UI 结果查看和 CI quality gate；DeepEval 官方文档明确展示了 `pytest`/`assert_test()`、自定义 metric 与 CI 执行。两者都不能据此推出“医疗 P0 默认规则”或“跨 run Finding 生命周期”已经内置。

### 3. 场景与相对差异

#### 3.1 把医疗 P0 从平均分中剥离

**痛点**：候选版总体指标上升，仍可能漏掉急症升级或出现危险建议。通用阈值可以表达失败，但项目团队仍需自行定义哪些失败一票否决。

**MedGate 方案**：测试病例在检查点层预设严重度；任一候选版未解决 P0 直接 `BLOCKED`，加权总分只用于定位，不参与否决权。

**相对优势**：相比从零编写 Promptfoo assertion、DeepEval metric 或通用平台 scorer，MedGate 把预问诊 P0 分类、证据字段和门禁优先级作为产品默认值。

**不足**：这些规则尚未经执业医师复核，也没有真实团队验证，当前只能作为作品集方法演示。

#### 3.2 让需要等人的复核进入发布流程

**痛点**：自动 Judge 可能低置信度或缺证据；CI 是同步执行环境，医疗确认却可能需要人工处理。

**MedGate 方案**：CI 只消费 `PASSED / BLOCKED / REVIEW_REQUIRED`。待复核 P0 失败关闭并返回 `BLOCKED`；其他低置信度或必审项返回 `REVIEW_REQUIRED`。复核完成后通过绑定稳定内容哈希的 ReviewPack 重跑；`completed` 只表示评测流程完成，不等于允许发布。

**相对优势**：不是“MedGate 才有人工复核”，而是它为医疗 P0 预设了 Finding 状态、必审条件、证据要求和 CI 三态映射。

**不足**：首发只做 GitHub Actions 示例，不覆盖其他 CI 平台、审批系统和管理员绕过治理。

#### 3.3 分开保存发布历史与问题闭环

**痛点**：新版本修复了旧问题，不应该把旧候选版当时的发布结论改成绿色。

**MedGate 方案**：GateDecision 绑定一次 run；同一 run 的人工纠错可以追加新 decision，但已发布快照不可变。跨 run 的回归通过只关闭 Finding，不改旧 run 的历史报告。

**相对优势**：领域质控人员可以同时回答“当时为什么被拦”和“后来是否修好”。

**不足**：这是一套计划中的产品语义，不代表现有竞品无法通过自定义 schema 实现。

### 4. 明确不竞争的地界

- Trace、成本与延迟可观测性：优先使用 Langfuse、Braintrust 等成熟平台。
- 通用模型/Prompt 大规模对比：优先参考 Promptfoo、DeepEval。
- 红队攻击面与安全扫描：不在首发范围。
- 多租户、Prompt 托管、线上监控和商业化团队协作：不在作品集 MVP 范围。

未来更合理的方向不是替代这些工具，而是让 MedGate 成为其上层的医疗策略与工作流适配层。

## 三、结论与面试防守

### 1. 结论

MedGate 的机会不在“做一个更全的 LLM 评测平台”，而在“把通用评测能力收敛成可解释的医疗发布决策”。它的竞争力只能来自三部分：经过验证的领域病例与 P0 规则、严谨的 run/Finding 生命周期、以及真实接入发布流程的能力。

### 2. 我从竞品分析改变了什么

1. 我放弃了把人工复核、可复现或 CI 当作独有功能，因为头部工具已经覆盖。
2. 我把首发从 8 个管理页面裁成 4 个证据页面，把完整 CRUD 放进 Backlog。
3. 我新增三态 CI 契约，因为没有发布检查的“门禁”只是报告。
4. 我明确未来可复用通用框架作为执行器，而不是重复建设 Trace、Prompt 管理和指标生态。

### 3. 面试追问

| 追问 | 回答 |
| --- | --- |
| 为什么不用 Promptfoo？ | Promptfoo 很适合做执行器和 CI assertion；MedGate 要证明的是医疗 P0、人工复核和历史证据如何形成产品工作流。后续可以接 Promptfoo，而不是与它争做通用 runner。 |
| Braintrust、Langfuse 也有人审，差异在哪里？ | 差异不在“有人审”，而在预问诊 P0 规则、必审状态、三态 CI 和 run/Finding 分离是否开箱即用。当前只是待实现假设，需要用 Demo 和用户反馈验证。 |
| 你的 P0 谁定义？ | Demo 由本人基于公开医疗安全常识编写并明确标注未获医师验证；真实产品必须由医疗质控负责人维护、评审和版本化。 |
| 这个门禁真的不可绕过吗？ | 只在目标仓库启用必要检查和分支保护后，普通合并流程无法绕过；管理员关闭规则属于组织治理边界。产品内不能靠改总分或普通备注放行 P0。 |
| 哪些是你自己的判断？ | 范围裁剪、P0/总分解耦、run/Finding 分离、公开只读回放和 CI 三态是我的产品取舍；竞品事实来自官方资料，文稿结构由 AI 辅助整理。 |

### 4. 证据与缺口

- [Langfuse Overview](https://langfuse.com/docs)：Trace、Prompt、Evaluation、Dataset、Experiment 等官方能力。
- [Langfuse Annotation Queues](https://langfuse.com/docs/evaluation/evaluation-methods/annotation-queues)：领域专家人工评分与评论。
- [Braintrust Evaluation](https://www.braintrust.dev/docs/evaluate)：不可变 Experiment、CI/CD 与自动评分。
- [Braintrust Human Review](https://www.braintrust.dev/docs/annotate/human-review)：人工复核流程。
- [Promptfoo CI/CD](https://www.promptfoo.dev/docs/integrations/ci-cd/)：质量门禁、GitHub Actions 等集成。
- [Promptfoo Assertions](https://www.promptfoo.dev/docs/configuration/expected-outputs/)：确定性、阈值与模型评分断言。
- [DeepEval Introduction](https://deepeval.com/docs/introduction)：Python/pytest 评测与 CI/CD。

本稿没有一手试用和用户访谈，属于公开资料第一版。编码前至少应亲自运行 Promptfoo 与 DeepEval 的最小示例；对外发布前再次核对功能与版本。
