# MedGate 开发计划（2026-08-18）

| 项目 | 内容 |
| --- | --- |
| 用途 | T-041 下一阶段开发排期：从当前基线（M3.1 完成、并发改造已实现、V1→V3 提示词闭环）推进到多维度测试集、M3.2/M3.3 与公开发布面 |
| 硬截止 | **2026-08-19（周三）外服云终面现场展示本项目**，可用状态须在 08-18 收工前就绪（T-041/T-042 看板记录） |
| 排序原则 | 演示稳定性 > 测试集资产 > 判定能力增强（RAG/引用门禁）> 发布面；依赖关系决定顺序，不并行开多条战线 |
| 基线日期 | 2026-08-18（本地 main=6f8e219，领先 origin/main 6 个提交；110/110 测试通过） |

---

## 一、当前基线（已验证事实，不重复推导）

1. **已完成并验证**：M1 原型、T0 版本化资产（12 例 / 24 fixture / manifest）、M2 离线 Runner/CLI/SQLite、本地 FastAPI + SSE 流式 live run、M3.1 Agent 配置包纯文本 Skill 评测（Fake Client 验收）、病例级并发执行 + 429 退避重试 + 预算失败关闭（`6f8e219`，110/110 测试）。
2. **提示词资产**：V1→V2→V3 单变量迭代闭环（`08_`/`10_`/`11_` 文档），V3 已通过真实重跑验收（run-20260817-144511：case-007 修复、12/12 无回归、0 findings）；case-002 复审语义误报已在代码层修复（`79c821b`）。
3. **真实运行记录**：已完成 **15 次真实 live run**（08-14 五次、08-17 十次，SQLite `live_submissions` completed 计数），其中至少 8 次进入 bad case 归因迭代记录；最新为 run-20260817-144511（55 次外调，Gate=REVIEW_REQUIRED）。规则层曾两次捕获 Judge 幻觉（case-002，run-022535 与 run-062321），是面试叙事的核心证据。
4. **未完成项**（散落在 README/PROGRESS/BLOCKED/看板，本计划统一收口）：
   - 多维度测试集 schema 设计与构建（看板唯一下一步，`12_` 调研已定四维分布与来源映射）；
   - 并发改造的真实性能验收（墙钟/429，需大测试集 + 用户 Key）；
   - case-002 复审修复的全量 12 例回归重跑；08-17 run 的 pending_review findings 人工复核（用户侧）；
   - v2 Agent 包路径真实 DeepSeek smoke（M3.1 最终放行条件，需用户确认外发；当前仅有 Fake Client 验收）；
   - M3.2 SQLite FTS5 中文 RAG（`knowledge_search`）、M3.3 推荐 Tool（`recommend_services`）、LIVE-2 引用门禁；Skill 参考 Markdown 按显式引用加载（PROGRESS 未覆盖项）；
   - GitHub Actions CI、公开脱敏导出、usage 持久化（token/费用）、公开部署；
   - 看板还登记一项用户提出、范围待确认的需求：页面支持提示词版本迭代（保存/生成新版→重跑→对比），本计划收口到 P2-5；
   - 明确不排期（本周期不做，见四.5）：Promptfoo/DeepEval 本地最小示例、mark-fixed 后端动作与失败任务恢复、v1 全局 422 错误 shape 历史兼容收口；
   - 本地 6 个提交未推送 GitHub（含并发特性与 V3 迭代文档，仓库当前落后），且 README 状态表停留在 M3.1（测试数 72 vs 实际 110、缺并发与 V1→V3 记录）。

---

## 二、阶段计划

### P0｜终面演示就绪（08-18 当天收工前，代码冻结）

**目标**：明天现场展示零翻车。本阶段**只做验证、彩排与文档状态同步，不改任何代码、规则与资产**。

| # | 任务 | 验收 |
| --- | --- | --- |
| P0-1 | 冒烟全绿：全量 unittest、`medgate validate`、资产 validate/smoke、原型 check/smoke、服务 `/health`、重启后 rule_hash 为最新 | 全部通过，结果与风险预案要点一并记入 PROGRESS.md（预案不只留在本文件） |
| P0-2 | README 状态同步（纯文档，不违反代码冻结）：测试计数 72→110、状态表补并发改造与 V1→V3 提示词资产、目录说明补 `10_~14_` 文档索引 | README 与实际状态一致，随 P0-4 一起提交 |
| P0-3 | 演示路径彩排（对齐 `00_项目说明.md` 3 分钟脚本）：启动（`启动MedGate.command` 或 `python3 -m medgate.api`）→ 首屏空状态 → 测评集详情（12 例 + 规则体系）→ **双 run 幂等回放（均不耗 Key，需预先验证幂等键可回放）**：run-20260817-022535（真实 BLOCKED + 4 个 P0 Finding + case-002 规则层抓 Judge 幻觉证据，叙事高点）与 run-20260817-144511（V3 验收 run，12/12 candidate 全绿 + 0 findings）→ 病例详情三层证据 → BLOCKED 历史快照 → 三态 CLI 语义 | 用户完整走一遍无阻断 |
| P0-4 | 推送本地提交（含 P0-2 README 同步）到 GitHub（面试官可能现场查仓库；**推送前须用户确认**） | origin/main == 本地 main |
| P0-5 | 风险预案：无网/Key 失效时改用本地报告快照离线讲解；服务异常时用 `python3 -m http.server 18181` 静态路径兜底展示测评集详情 | 预案要点写入 PROGRESS.md（随 P0-1），不新增代码 |

**明确不做**：不动规则词表、不动提示词、不启动 M3.2、不重构。除 P0-2 文档同步外无任何代码/资产改动，全部开发放到终面之后。

### P1｜多维度测试集 v1（08-20 ～ 08-26，看板唯一下一步）

**目标**：按 `12_` 调研落地方案构建 150 条四维测试集（FAQ 60 / 复杂疾病 38 / 多轮 30 / 边界 22），这是后续并发验收、RAG、引用门禁的公共依赖。

| # | 任务 | 说明 |
| --- | --- | --- |
| P1-1 | Schema 设计（0.5–1 天）＋判定引擎改造（1–2 天，独立计工作量） | case 结构新增 `dimension`、FAQ 标答字段、多轮上下文（2–4 轮）、边界类型标签；新增判定类型不是免费开关：FAQ「标答命中」与边界「拒绝合规」（角色冒充/下诊断/开处方/超范围）需要规则层 + Judge 提示词 + 复审 + 回归测试四层同步改造，历史审计「30 例 25–50 小时」的教训主要就在这类转换成本；**决策点**：新维度是否需要双版本离线 fixture（建议：仅每维挑 2–3 例关键 case 进 fixture 供 CI，其余 live-only，避免 150×2 fixture 的内容工作量失控——12 例深 case 集仍是离线回放核心） |
| P1-2 | FAQ 维 60 条（自建） | 标问 + 3–5 相似问变体 → 标答；零 license 风险，产品叙事最强，先做 |
| P1-3 | 边界维 22 条 | 自建（人机识别/开处方/下诊断/超范围）+ NOHARM 开源子集改写（CC-BY-4.0，署名清单） |
| P1-4 | 复杂疾病维 38 条 | CMB-Clin 74 例筛选改写（Apache-2.0，署名），改写为 case 格式并标注期望动作 |
| P1-5 | 多轮维 30 条 | CMB-Clin 多轮化 + Chinese-medical-dialogue 筛选改写（Apache-2.0），补跨轮上下文规则 |
| P1-6 | 每维收口 | 每维完成后跑离线校验 + 小样本 live 冒烟（用户 Key，≤10 例），维度独立可用即提交，不追求一次全量 |

**验收分级**（对冲内容工作量风险）：**v1 = 四维中任意两维（≥100 条）可用即视为 P1 达标**，P2 可对已就绪维度滚动开始；**v1.1 = 补足 150 条全量**，允许滑到 P2 期间完成。共同硬性要求：manifest 版本化与校验脚本覆盖新字段、validate/smoke 全过、许可证署名清单（`NOTICE` 或文档内章节）落盘、GPL/无 license 来源（MedDG/imcs21）零文本进入仓库。

### P2｜大规模真实运行与验收清欠（08-27 ～ 09-01，按维滚动）

| # | 任务 | 说明 |
| --- | --- | --- |
| P2-1 | 全量真实运行（并发 4–8，用户 Key） | 落实 `13_` 验收标准：墙钟 ≤15 分钟（目标 5–10）、429 退避行为、预算口径实测；产出第一份多维度基线报告。**不等 150 条全齐**：P1 v1 达标的两维（≥100 条）即可先跑首轮滚动验收，全量到位后补终轮 |
| P2-2 | Bad case 归因与迭代 | 大运行必然暴露新误报/漏报；按 `11_` 七步模板逐条定性（规则误报/真阳性/Judge 漂移），单变量修复并固化回归。工作量按 08-17 实测外推（12 例一轮归因约大半天），150 条首跑的清欠允许溢出到 09-01 |
| P2-3 | 回归清欠 | case-002 复审修复的全量 12 例重跑回归（遗留项）；P1 新增规则的对抗语料补充 |
| P2-4 | 用户侧：pending_review 人工复核 | 新旧 findings 的复核闭环与 Gate 重算（Agent 准备复核包，裁决归用户） |
| P2-5 | 页面提示词版本迭代功能（范围待确认） | 看板登记的用户需求：保存/生成新版提示词→重跑→对比。终面后先与用户确认范围再排期；若确认，优先做「版本列表 + 一键填入 Baseline/Candidate」最小切片，复用 `11_` 模板的版本登记结构 |

### P3｜v2 真实放行 → M3.2 中文 RAG → LIVE-2 引用门禁（08-30 ～ 09-05）

| # | 任务 | 说明 |
| --- | --- | --- |
| P3-0 | v2 Agent 包路径小规模真实 DeepSeek smoke | **前置于一切 v2 新能力**：M3.1 至今只有 Fake Client 验收（PROGRESS 明确「未最终放行前不启动真实 Key 外发」），RAG 与 LIVE-2 都要建在 v2 路径上，必须先用 2–3 例纯文本 case 完成真实外发 smoke（需用户确认），避免 P3/P4 建在未放行路径上整体返工。通过即宣告 M3.1 真实验收 |
| P3-1 | RAG 方案 spike（0.5–1 天） | 第一步先验证本机 sqlite3 是否编译启用 FTS5（macOS 系统 Python 常未启用，未验证前不谈选型）；再做中文检索方案选型（字符 bigram vs 外部分词依赖，倾向零外部依赖）；知识库条目带稳定定位符（result_id/chunk_id），对齐 V3 提示词已定义的 `[K#]` 引用合同 |
| P3-2 | 知识库资产与 `knowledge_search` Skill | 自建脱敏药品/分诊知识条目；检索真实发生并进 trace；v2 Agent 包路径接入；同轮收口 PROGRESS 遗留的「Skill 参考 Markdown 按显式引用加载」 |
| P3-3 | LIVE-2 引用门禁（拆两半） | ①**可先行、不依赖 RAG**：「无知识库证据却输出具体医学事实」违规捕获——这正是 BLOCKED.md LIVE-2 记录的原始场景（两版均声明无知识库来源却输出具体医学事实），无知识库时即可检测，若 P1/P2 期间有富余可提前实现（先只进报告不计入 Gate，降低误杀风险）；②**依赖 P3-1/P3-2**：引用覆盖率与引用蕴含关系，需要真实知识库与稳定定位符 |
| P3-4 | 顺带收口 | `is_negated` 逗号截断问题重评（M3.1 遗留已知边界，届时规则层已稳定，重评成本低） |

**验收**：P3-0 smoke 通过并留 run 记录；RAG 检索与引用真实进 trace；构造「无证据输出医学事实」用例被拦截；离线/CLI 路径零回归；药物咨询场景（V3 提示词）在真实知识库 + 真实模型下端到端跑通（外发确认见四.7）。

### P4｜M3.3 推荐 Tool + 三类 Skill 统一回归（09-06 ～ 09-10）

| # | 任务 | 说明 |
| --- | --- | --- |
| P4-1 | `recommend_services` 推荐 Tool | 科室/服务类别推荐，真实 Tool 调用进 trace（禁止预置推荐结果，`09_` 方案红线） |
| P4-2 | 三类 Skill 统一正式回归（formal repeated） | 纯文本 / RAG / 推荐三类 Skill 在真实 DeepSeek 下按 `09_` 方案做正式重复回归；通过后 M3.1–M3.3 合并声明真实验收 |

### P5｜发布面（09-11 ～ 09-15，可按面试节奏提前）

| # | 任务 | 说明 |
| --- | --- | --- |
| P5-1 | GitHub Actions CI（**可提前**：依赖已齐备，半天工作量，可插入任意空档；若投黑客松应提前做，带 CI 的仓库更有卖相） | 离线 fixture 回放 + 三态退出码（0=PASSED/1=BLOCKED/2=REVIEW_REQUIRED），对齐 `00_项目说明.md` 的 CI 叙事 |
| P5-2 | 公开脱敏报告导出 | 导出不含 Key、不含本地路径的报告快照，供作品集页面引用 |
| P5-3 | usage 持久化 | DeepSeek 客户端落盘上游 `usage`，本地报告可核算 token/费用（补齐 README「尚未实现」第 4 条） |
| P5-4 | 公开静态部署 | 只读作品集页面（冻结回放 + 脱敏报告），部署目标（GitHub Pages 或等价）实施时定 |

---

## 三、可选项：Devpost Hack for Humanity（截止 09-04）

- 要求：可运行软件 + GitHub 源码 + 最长 4 分钟视频；允许个人参赛。
- 若投递：P1/P2 须压缩到 **09-01 前完成**，P5-1 CI 与 P5-4 公开部署须提前，另需 0.5–1 天制作视频；撞期处置：P3 顺延至提交（09-04）之后开始。
- All Things Agentic（08-31 截止）要求 Gemini 3.5+，与本项目 DeepSeek 技术栈不符，**不投**。
- **决策截止 08-25**，由用户拍板；不投则按主线节奏执行。

---

## 四、贯穿性约束（所有阶段护栏）

1. **失败关闭语义不可破坏**：语义层判 fail 必须 fail；词表放宽只作用于规则层字面误报。
2. **单变量迭代**：提示词/规则/复审契约每次只改一处，按 `11_` 模板留档。
3. **许可证红线**：Apache/MIT/CC-BY/CC0 可改写并入（署名）；GPL 系与无 license 仅参考，零文本入库。
4. **数据边界声明保持**：合成与改写条目一律保留「未经执业医师复核，不得用于临床决策」口径。
5. **非目标不做**：完整 CRUD、PostgreSQL、多租户、RBAC、部分成功断点续跑（`13_` §七非目标）。另有三项**本计划周期内不做**（后置，非永久放弃）：v2 异步/SSE（README 后续里程碑口径，本周期 v2 维持 `synchronous-local-demo`）、Promptfoo/DeepEval 本地最小示例（BLOCKED 后置工作）、mark-fixed 后端动作与失败任务恢复及 v1 全局 422 shape 历史兼容收口（PROGRESS 既有边界）。
6. **每阶段收口动作**：全量测试 + 资产校验 + PROGRESS/BLOCKED 同步 + 独立提交（可回滚）+ 看板 T-041 更新；复杂阶段（P1 schema、P3 RAG、P5 CI）按工作区 SOP 触发对抗性审查。
7. **用户侧阻塞项**（Agent 只准备不替代）：真实 Key 外发确认（P2-1 / P3-0）、pending_review 复核裁决（P2-4）、GitHub 推送确认（P0-4）、黑客松决策（08-25）、执业医师复核（长期，不阻塞发布，声明口径已覆盖）。

---

## 五、风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 150 条内容工作量超预期（历史上 30 例曾被审计为 25–50 小时） | P1 滑期 | 分维独立验收、FAQ/边界先做（零 license 风险、可分割）、fixture 仅覆盖关键 case；每维完成即独立可用，不追求一次全量 |
| 大运行成本（150×2 ≈ 1500+ 次调用）与 429 放大 | 费用与墙钟 | 预算封顶已实现；先 10 例小样本，再全量；并发度可配可回退串行 |
| RAG 中文分词质量不达预期 | P3 滑期 | P3-1 先 spike 再定方案；检索质量不达标时降级为精确匹配 + 别名表，不阻塞 LIVE-2 主链路 |
| 求职面试行程打断开发 | 整体滑期 | 各阶段独立可暂停/恢复，每阶段收口都有提交回滚点；P0 之后无跨阶段强耦合 |
| 明天终面演示失败风险 | 面试结果 | P0 全部是验证与预案，不引入改动风险；离线快照兜底 |

---

## 六、计划外变更

本计划是排期基线，不是硬合同：终面结果、新面试邀约或用户新指令都可能重排优先级（如终面被问及 RAG/引用门禁，P3 可提前）。变更时更新本文件并同步看板 T-041，不静默改向。
