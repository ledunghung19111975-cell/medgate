# MedGate 项目审核报告

| 项目 | 内容 |
| --- | --- |
| 审核对象 | MedGate 医疗 AI 发布门禁（求职作品集） |
| 审核范围 | 全部 11 个文件：6 份方案文档 + 原型 + 检查脚本 + 进度/阻塞文件 |
| 审核日期 | 2026-08-12 |
| 审核模式 | 主 Agent 单人审核，全文逐文件读取 |
| 处置 | 只出意见，未修改原文 |

---

## 一、总评

**作为产品设计文档，质量很高；作为面试交付物，已从"零可运行产物"进步到可点击原型，但距离"能证明本人 coding"仍有明显差距。**

这个项目有一个非常强的产品内核——"医疗 P0 硬门禁独立于加权总分"，并且围绕这个内核设计了完整的证据链：双版本回放 → 三层评测 → 病例矩阵 → 逐轮证据 → 人工复核 → Gate 重算 → 报告快照 → CI 退出码。这条链路在逻辑上自洽，在医疗场景中可辩护，是全部卖点所在。

v0.1 审核意见提出的 7 个 P0 问题已全部在 v0.2 中落实修改，文档的迭代质量值得肯定。当前的主要矛盾是：**文档描述的系统深度（FastAPI + SQLite + React + CLI + GitHub Actions + 哈希绑定 ReviewPack）与实际交付物（一个 724 行单文件 HTML 原型）之间的差距仍然很大。** 诚实的边界声明缓解了这个问题，但面试官最终想看的是"能不能点开"。

**成熟度判断**：

| 维度 | 评分 | 说明 |
| --- | --- | --- |
| 问题定义与产品判断 | 9/10 | P0/总分解耦、run/Finding 分离、回放一等模式都是硬判断 |
| 文档严谨度 | 8.5/10 | 术语、状态机、验收标准、需求追踪矩阵齐全 |
| 前序审核跟进 | 9/10 | v0.1 的 7 个 P0 全部落实，改动质量高 |
| 竞品与差异化 | 8/10 | 诚实、有立场、划清了不做的边界 |
| 原型完成度 | 5/10 | 主链路可走通，但 11/12 病例无实质内容，存在代码 bug |
| 可演示性 | 6/10 | 3 分钟脚本能走通，但只有 1 个病例有真实对话 |
| 综合面试竞争力 | 7/10 | 文档是强资产，但需要尽快补上可运行后端 |

---

## 二、优点（真优点）

### 1. v0.1 审核意见的跟进质量极高

这是整个项目最亮眼的地方。v0.1 审核提出了 7 个 P0、6 个 P1、9 个 P2 问题，v0.2 文档逐条落实：

| v0.1 问题 | v0.2 落实情况 |
| --- | --- |
| P0-1 范围超载（8 页面→4 页面，30 例→12 例） | ✅ 需求 §3 和技术 §17 已裁剪 |
| P0-2 门禁归属对象未定义 | ✅ GateDecision/Finding 分离已写透，需求 §4.1 和技术 §8.2 各有一段 |
| P0-3 演示路径锁死（BLOCKED 出不了报告） | ✅ "必审项已处理"已定义，BLOCKED 可导出报告 |
| P0-4 内容工作量零预算 | ✅ 12 例 + gen_fixture.py 工具规划 |
| P0-5 不接 CI | ✅ CLI 退出码 + GitHub Actions 示例 + ReviewPack 桥接 |
| P0-6 缺竞品分析 | ✅ 04_竞品与差异化.md 独立成文 |
| P0-7 通篇没有"我" | ✅ 00_项目说明.md + 05_关键决策记录.md 第一人称 |

这不是表面修改——每个 P0 的修改都触及了方案的核心逻辑，且修改后文档内部一致性保持得很好。

### 2. P0 硬门禁与加权总分的解耦是真正的产品判断

不是喊口号，而是落到了数据结构和算法上：`gate_decisions` 只读 active attempt 下的结构化事实，带 `input_hash`，优先级 `BLOCKED > REVIEW_REQUIRED > PASSED`，明确"不提供一键强制放行"。面试时这是一个可以展开 5 分钟的完整叙事。

### 3. "无密钥回放"作为一等执行模式

这一刀直接解掉了"作品集 Demo 需要密钥所以没法给面试官点"的死结。`external_call_count=0` 作为可验证的验收口径，把"离线"从形容词变成了机制。

### 4. 诚实边界守得住

全文没有把假设写成成果。"性能数字是验收目标，不是已验证成果"、"病例未经执业医师复核"、"当前只能证明方案与 M1 原型已形成"——这些声明贯穿所有文档，面试时不会翻车。

### 5. 竞品分析有立场

不声称"有人审"或"能进 CI"是独有能力，而是承认头部工具已覆盖，把差异收窄到医疗 P0 规则、三态门禁语义和病例级证据呈现。主动划出不做的地界，比声称什么都做可信。

---

## 三、问题清单

### P0｜不改会影响面试交付

---

#### P0-1 原型代码 bug：病例详情页标题未插值

**位置**：`prototype/index.html` 第 590 行

```javascript
${pageHeader("病例证据 · ${item.id}", "病例详情", ...)}
```

**问题**：`pageHeader` 的第一个参数使用了双引号字符串 `"病例证据 · ${item.id}"`，JavaScript 模板字面量插值只在反引号中生效。因此页面会显示字面文本 `病例证据 · ${item.id}` 而非实际的 `病例证据 · case-003`。

**影响**：面试官点开任意病例详情时，面包屑下方的页头标题会暴露模板语法错误，直接拉低专业感。

**修复**：改为反引号或字符串拼接：

```javascript
pageHeader(`病例证据 · ${item.id}`, "病例详情", ...)
```

---

#### P0-2 11/12 病例无实质内容，演示路径只能走 1 个病例

**位置**：`prototype/index.html` 第 446–462 行 `detail` 对象

**问题**：`detail` 对象只包含 `case-003` 的对话、证据和 Finding 数据。其余 11 个病例全部走 `defaultDetailFor(item)` 生成的占位文本（"Baseline v1 已按'CP-XX'完成预置安全检查"），没有真实的双栏对话、检查点证据或 Finding 信息。

**影响**：

- 面试官点开 case-001（总览页表格第一行）会看到占位文本，第一印象是"内容空洞"。
- 整个演示只能在 case-003 上展开，面试官如果随机点击其他病例，体验会断崖式下降。
- 需求方案 §8 明确要求 4 个关键病例，但原型中只有 case-003 有关键内容。

**建议**：

1. 至少为 4 个关键病例（case-001～004）编写真实的双栏对话和检查点证据。
2. 为 2–3 个普通病例编写有区分度的对话（不一定要有 P0，但要有真实医学内容）。
3. 其余病例可以用 `defaultDetailFor` 但需要让占位文本更具体，而不是"已按 CP-XX 完成预置安全检查"这种空话。

---

#### P0-3 文档与实现差距大，且文档未明确标注"这是计划"

**位置**：技术方案全文

**问题**：技术方案描述了 FastAPI + SQLAlchemy + SQLite + React + shadcn/ui + CLI + GitHub Actions 的完整技术栈，包含 10 张数据表、2 套状态机、伪代码、API 设计和测试策略。但实际交付物是一个无后端、无数据库、无构建工具的单文件 HTML 原型。

虽然 00_项目说明.md 和 PROGRESS.md 都声明了"M2 回放内核待开发"，但技术方案文档本身的语气是"首发技术边界已冻结"——一个面试官如果只读技术方案，会以为这些都已经实现或即将实现。

**建议**：

1. 在技术方案开头加一个醒目的状态标注，例如一个表格：

| 模块 | 文档状态 | 实现状态 |
| --- | --- | --- |
| 4 页面交互原型 | v0.2 已完成 | ✅ prototype/index.html |
| FastAPI + SQLite | v0.2 已设计 | ❌ M2 待开发 |
| CLI + CI | v0.2 已设计 | ❌ M3 待开发 |
| ... | ... | ... |

2. 或者在每个章节标题后加 `[计划]` / `[已实现]` 标签。

---

### P1｜会被追问或影响演示质量

---

#### P1-1 六维评分与病例级分数脱节

**位置**：`prototype/index.html` 第 464–471 行 `dimensions` 数组 + 第 431–443 行 `cases` 数组

**问题**：六维评分（风险识别、信息完整等）的 baseline/candidate 分数是全局硬编码的固定值（如风险识别 78→82），与 12 个病例的逐例分数（case-001: 82→83）之间没有任何计算关系。面试官如果问"这个维度分是怎么从 12 个病例汇总出来的"，目前无法回答。

**影响**：需求方案 §FR-04 定义了六维权重（30%/20%/20%/15%/10%/5%）和评分映射规则（规则通过/失败→100/0，Judge 0–4→0/25/50/75/100），但原型中的数字不遵循这些规则。

**建议**：

1. 在原型中加一个注释说明"六维分数为预置回放值，实际计算规则见需求方案 FR-04"。
2. 或者让六维分数从 12 个病例的分数按权重反推，至少在数字上对得上。

---

#### P1-2 原型不响应移动端，min-width: 1180px

**位置**：`prototype/index.html` 第 33 行 `body { min-width: 1180px; }`

**问题**：面试官如果在手机或窄屏笔记本上打开链接，会出现横向滚动条，体验很差。

**建议**：首发改为 `min-width: 320px` 并加媒体查询，或至少改为 `max-width: 1200px; margin: 0 auto;`。如果时间紧，至少把 `min-width` 去掉。

---

#### P1-3 搜索框每次输入触发全量重渲染

**位置**：`prototype/index.html` 第 707–709 行

```javascript
document.addEventListener("input", (event) => {
  if (event.target.dataset.filter === "search") {
    state.search = event.target.value; render(); ...
  }
});
```

**问题**：每次按键都调用 `render()` 重建整个页面 innerHTML，会导致输入框失焦。代码中用 `input?.focus()` + `setSelectionRange` 补救，但这是一种脆弱的 workaround。12 个病例的数据量下不会卡，但做法不好。

**影响**：功能可用但代码质量会被工程面试官质疑。

**建议**：搜索筛选只重渲染表格 tbody，不重渲染整个页面。或者用 `debounce`。

---

#### P1-4 "已修复待回归"状态在原型中无法演示

**位置**：需求方案 §FR-03 第三层 + 技术 §8.2 Finding 状态机

**问题**：需求和技术方案花了大量篇幅设计 `fixed_pending_regression → regression_passed` 的跨 run 回归语义，这是 D-06 决策的核心。但原型中无法演示这个流程——没有"标记已修复"按钮，没有关联新 run 的入口，也没有 Finding 生命周期变化的展示。

**影响**：面试官如果问"你说的跨 run Finding 闭环怎么演示"，目前答不上来。这是文档中最有深度的设计点之一，但原型完全没有覆盖。

**建议**：在病例详情页的复核面板中加一个"标记已修复待回归"按钮（私有模式），点击后展示 Finding 状态变化和"旧报告仍 BLOCKED"的提示。不需要真的跑新 run，只需要演示状态语义。

---

#### P1-5 GitHub Actions 示例中 python-version "3.12" 与本机环境不一致

**位置**：技术方案 §11.2 第 399 行

```yaml
python-version: "3.12"
```

**问题**：本机 Python 是 3.13.x，技术方案写 3.12 没有问题（GitHub Actions 的 setup-python 支持），但如果面试官问"你用的什么版本"，需要对得上。这不是错误，但需要准备好答案。

**建议**：确认实际开发时使用的 Python 版本，保持文档与实现一致。低优先级。

---

### P2｜细节问题，不阻塞面试

| # | 位置 | 问题 | 建议 |
| --- | --- | --- | --- |
| P2-1 | 原型第 590 行 | `pageHeader` 中 `${item.id}` 未插值（同 P0-1） | 改双引号为反引号 |
| P2-2 | 原型第 548 行 | 总览页"报告快照结论"硬编码为"BLOCKED · 不可变历史事实"，即使切换到 PASSED 预览也不变 | 让快照结论跟随 `gateState()` 变化，或加注释说明快照是历史事实 |
| P2-3 | 原型第 542 行 | 候选版总分"81.5"和基线"78.4"在多处硬编码 | 提取为常量或从 `dimensions` 计算 |
| P2-4 | 原型第 686 行 | 提交 run 后 toast 说"已保存 run-demo-002"，但没有实际的 run 状态变化 | 可以在提交后让总览页显示"running"状态 |
| P2-5 | 04_竞品与差异化.md 第 51 行 | Promptfoo 和 DeepEval 的"人工复核"标注"本稿未核实完整队列" | 编码前至少跑一次 Promptfoo 和 DeepEval 的最小示例，核实人工标注能力 |
| P2-6 | 05_关键决策记录.md | 全部 10 条决策的"验证"字段都是未来条件 | 在每条后加一个"当前状态"字段标注"未开始/进行中/已完成" |
| P2-7 | 需求方案 §8 第 329 行 | "合计 12"的"关键病例"列写 4，但 case-001～004 都是 P0，标题说"关键病例 3" | 需求 §8 表头"关键病例"列 case-003 标 3 但实际有 4 个 P0（001～004），需明确"关键病例"和"P0 病例"的定义差异 |
| P2-8 | 技术方案 §6.4 第 191 行 | `run_input_hash` 排除 ReviewPack 的设计很好，但 `review_pack_hash` 的计算时机（导入后单独记录）与 GateDecision 的 `input_hash`（纳入已校验 ReviewPack 哈希）之间的关系可以更清晰 | 加一个时序图或更明确的文字说明 |

---

## 四、文档一致性检查

### v0.1 审核意见落实验证

| v0.1 编号 | 问题摘要 | v0.2 状态 | 验证位置 |
| --- | --- | --- | --- |
| P0-1 | 范围超载 | ✅ 已落实 | 需求 §3.1/§3.2、技术 §17 |
| P0-2 | 门禁归属对象未定义 | ✅ 已落实 | 需求 §4.1、技术 §8.2、决策 D-06 |
| P0-3 | 演示路径锁死 | ✅ 已落实 | 需求 §4 术语"必审项已处理"、§FR-06 生成条件 |
| P0-4 | 内容工作量 | ✅ 已落实 | 需求 §8（12 例）、决策 D-02、技术 §17 T2.5 |
| P0-5 | 不接 CI | ✅ 已落实 | 需求 §FR-07、技术 §11、决策 D-07 |
| P0-6 | 缺竞品分析 | ✅ 已落实 | 04_竞品与差异化.md |
| P0-7 | 通篇没有"我" | ✅ 已落实 | 00_项目说明.md、05_关键决策记录.md |
| P1-1 | M/T 编号不一致 | ✅ 已落实 | 技术 §17 有 M↔T 映射表 |
| P1-2 | 并发/超时字段冲突 | ✅ 已落实 | 任务弹窗只剩 3 个字段 |
| P1-3 | 缺 users/roles 表 | ✅ 已落实 | 技术 §7 `actors` 表 |
| P1-4 | FR-09 规则 1 状态不完整 | ✅ 已落实 | 需求 §FR-06 gate-v1 规则 1/2 |
| P1-5 | 自建 UI 组件层 | ✅ 已落实 | 技术 §4 改为 shadcn/ui |
| P1-6 | 访客写权限冲突 | ✅ 已落实 | 决策 D-03 公开只读 |
| P2-1～P2-9 | 9 项细节问题 | 大部分已落实 | 逐条验证见下 |

**P2 逐条验证**：

| v0.1 P2 | v0.2 状态 | 说明 |
| --- | --- | --- |
| P2-1 evaluation_results supersede | ✅ | 技术 §7 表中 evaluation_results 归属 attempt，通过 attempt.is_active 推导 |
| P2-2 API 命名风格 | ✅ | 技术 §12 统一为 `{resource}/{id}:{action}` |
| P2-3 token/cost 字段 | ✅ | 首发删除成本相关 UI，回放模式无成本 |
| P2-4 数据来源死选项 | ✅ | 需求 §8 只保留自编合成 |
| P2-5 一句话定位过长 | ✅ | 00_项目说明.md 有 ≤40 字版本 |
| P2-6 双库一致性测试 | ✅ | 技术 §16.2 集成测试只提 SQLite |
| P2-7 Idempotency-Key 唯一约束 | ✅ | 技术 §7 和 §12 明确 `(actor_id, idempotency_key)` |
| P2-8 状态机分支差异 | ✅ | 需求 §FR-02 状态机完整列出 |
| P2-9 回放包 10MB 限制 | ✅ | 首发不做导入 UI，限制不再相关 |

**结论**：v0.1 全部 22 个问题（7 P0 + 6 P1 + 9 P2）已在 v0.2 中落实，跟进质量极高。

### 文档间一致性

| 检查项 | 结果 |
| --- | --- |
| 需求与技术方案的 Gate 三态定义一致 | ✅ |
| 需求与技术方案的 Finding 状态机一致 | ✅ |
| 需求与技术方案的 Run 状态机一致 | ✅ |
| 需求与技术方案的 CLI 退出码一致 | ✅ |
| 需求与技术方案的 12 例分类一致 | ✅ |
| 00_项目说明的"M1 已完成"与 PROGRESS.md 一致 | ✅ |
| 04_竞品与差异化的能力表与 05_决策记录 D-08 一致 | ✅ |
| 需求 §8 表头"关键病例 3"与实际 4 个 P0 病例 | ⚠️ 轻微歧义（P2-7） |

---

## 五、原型检查脚本验证

运行 `node scripts/check_prototype.mjs` 结果：

```json
{
  "requiredMarkers": 7,
  "duplicateIds": 0,
  "inlineScripts": 1,
  "externalResources": false,
  "status": "ok"
}
```

✅ 7 个必需标记全部存在（总览、评测详情、病例详情、发布门禁、BLOCKED、REVIEW_REQUIRED、PASSED）
✅ 无重复 ID
✅ 恰好 1 个内联 script
✅ 无外部资源/网络调用
✅ 内联脚本语法检查通过

**注意**：检查脚本是结构性的，不覆盖运行时逻辑。P0-1 的模板插值 bug 不会被这个脚本发现。

---

## 六、改进建议（按优先级排序）

### 第一批：面试前必须修（预计 2–4 小时）

1. **修复 P0-1 原型 bug**：`caseView` 的 `pageHeader` 参数改双引号为反引号。5 分钟修完。
2. **补 3 个关键病例的对话内容**：为 case-001、case-002、case-004 编写双栏对话和检查点证据。每个病例 15–30 分钟，合计约 1.5 小时。
3. **技术方案加实现状态标注**：在开头加一个模块状态表，让面试官一眼看到哪些已实现、哪些是计划。30 分钟。
4. **原型去掉 `min-width: 1180px`**：改为响应式或至少去掉硬限制。15 分钟。

### 第二批：面试前最好补（预计 2–3 小时）

5. **在原型中演示"已修复待回归"状态**：复核面板加一个按钮，点击后展示 Finding 状态变化。1 小时。
6. **为 2–3 个普通病例写有区分度的对话**：不需要 P0，但要有真实医学内容。1 小时。
7. **六维分数加注释或从病例分反推**：让数字看起来有依据。30 分钟。

### 第三批：时间充裕时做

8. 跑一次 Promptfoo 和 DeepEval 的最小示例，更新竞品分析中的"未核实"标注。
9. 关键决策记录每条加"当前状态"字段。
10. 搜索框改为只重渲染表格。

---

## 七、面试防御评估

| 预期追问 | 当前准备度 | 备注 |
| --- | --- | --- |
| "这和 Promptfoo 有什么区别？" | ✅ 充分 | 04_竞品与差异化.md 有完整回答 |
| "门禁真的不可绕过吗？" | ✅ 充分 | 决策 D-07 和需求 §FR-07 有准确表述 |
| "P0 谁定义的？" | ✅ 充分 | 诚实标注未获医师验证 |
| "你的 P0 规则医学上对不对？" | ⚠️ 需准备 | 12 例病例未经执业医师复核，需准备好"这是产品机制演示"的话术 |
| "代码在哪？" | ⚠️ 不足 | 只有 HTML 原型，没有后端代码；需要强调"M2 回放内核是下一步" |
| "能不能现场跑一下？" | ❌ 不足 | 没有可运行的后端或 CLI；原型只能浏览器打开 |
| "跨 run Finding 闭环怎么演示？" | ⚠️ 不足 | 原型未覆盖，需用文档讲 |
| "六维分数怎么算的？" | ⚠️ 需准备 | 原型数字是硬编码，需能口述 FR-04 的计算规则 |

---

## 八、本次审核的未覆盖项

- 未评估 12 例病例的医学内容正确性（case-003 是唯一有实质内容的病例，其余为占位文本）。
- 未运行浏览器级 smoke test（仅运行了结构性检查脚本）。
- 未核对 Promptfoo / Braintrust / Langfuse / DeepEval 的当前最新功能（竞品分析基于 2026-08-12 的公开资料）。
- 未做多 Agent 对抗性审查（本审核为主 Agent 单人审核）。
