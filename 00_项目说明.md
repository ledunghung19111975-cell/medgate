# MedGate｜医疗 AI 发布门禁

> 让医疗 AI 的每次版本更新，在 P0 安全问题关闭前无法通过发布检查。

| 项目 | 当前事实 |
| --- | --- |
| 项目性质 | AI 产品经理面试作品集，计划由本人独立完成产品设计与全栈编码 |
| 当前阶段 | M1 原型、T0 资产、M2 离线 Runner、本地 FastAPI、手动提示词 live run、真实 DeepSeek 运行（15 次）、GitHub Actions CI 与多维度测试集 P1-1~P1-4（schema、FAQ 60 条、边界 22 条、复杂疾病 38 条）均已完成；P1-5 多轮维、M3.2 中文 RAG、M3.3 推荐 Tool 与 usage 持久化未开始 |
| 首发范围 | 12 个中文合成病例、双版本回放、人工复核、三态门禁、CI（离线回放 + 门禁断言，每次 push 运行）、多维度测试集（multidim-v1 / complex-v1，152 条） |
| 使用方式 | **clone 到本地自己跑**（依赖与命令见 `AGENTS.md` 第五节）；界面为 `prototype/index.html` 本地原型，须经 HTTP 打开，不支持 `file://`。**不做公开静态部署**（2026-08-20 决定）。页面不接收患者输入，写操作按运行环境收敛：非 loopback 打开时不出现真实评测入口 |
| 数据边界 | 主测试集与 FAQ／边界维为本人编写的合成病例；复杂疾病维（complex-v1）改写自 CMB-Clin 开源病例（Apache-2.0，见 NOTICE）。全部内容未经执业医师复核，不得用于临床决策 |

## 为什么做这个项目

医疗 Agent 的一次模型、提示词或工作流更新，可能让平均得分上升，却在少数高风险病例中产生严重回退。例如，新版本回答更流畅，但没有识别持续胸痛伴大汗需要立即就医。

我不想再做一个普通医疗聊天机器人，而是解决版本发布前更难被看见的问题：

- 如何用同一批病例复现基线版与候选版的差异；
- 如何让每条风险结论回到具体病例、轮次和检查点；
- 如何让 P0 不被平均分、阈值调整或普通备注稀释；
- 如何把需要医疗人员确认的异步复核接回 CI 发布流程。

## 首发 Demo 展示什么

访客不创建任务，只浏览一份已经完成并确认 P0 的预置运行。完整演示由 5 个页面组成：

1. **总览**：看到候选版被阻断，以及 P0/P1 数量和六维得分。
2. **测评集详情**：查看版本化测试集与 manifest 哈希链，含多维度测试集（multidim-v1 / complex-v1）的分层结构。
3. **评测详情**：查看 12 个病例的基线/候选结果矩阵，定位新增回退。
4. **病例详情**：双栏查看多轮对话、检查点、证据和人工复核记录。
5. **发布门禁**：看到 `BLOCKED` 原因、报告快照和 CI 检查结果。

在本机 loopback 环境打开时，页面额外开放真实评测入口：手动输入 Baseline/Candidate 提示词，由服务端调用固定 `deepseek-v4-flash`，对同一组 12 个合成病例生成真实回答和结构化 Judge，再将关键词/模式匹配与语义识别结果合并计算 Gate。提示词、模型、回答与两路评测证据均进入哈希绑定报告；Key 不进入页面或仓库。非 loopback 打开时该入口不出现，页面只展示冻结回放。

## 我做什么、不做什么

### 首发必须完成

- 12 个合成病例，其中 4 个关键病例、1 个可解释 P0 回退；
- 无密钥、无外连的双版本回放；
- 本地手动输入两版提示词的 live run；真实调用与公开回放相互隔离；
- 关键词/模式匹配、语义 Judge、人工复核三层结果；
- 病例矩阵、逐轮证据、P0 硬门禁和不可变报告快照；
- `PASSED / BLOCKED / REVIEW_REQUIRED` 三态 CLI、哈希绑定复核包与 GitHub Actions 示例。

### 首发明确不做

- Agent 版本、测试集和病例的完整 CRUD；
- 真实患者数据、真实医疗服务或临床有效性声明；
- 页面输入模型密钥、在线 Prompt 管理、并发/成本仪表盘；
- PostgreSQL、多租户、完整 RBAC、复杂并发恢复；
- 通用 Trace、Prompt 管理和全量 benchmark 平台。

这些能力不是被遗忘，而是因为不能提升首轮面试演示价值，被放入产品化 Backlog。

## 3 分钟讲解脚本

**0:00–0:30｜问题**
同一版本平均分更高，不代表关键病例更安全；医疗 P0 必须独立于总分。

**0:30–1:15｜看差异**
打开评测详情，展示 12 个病例的基线/候选矩阵，定位一个“基线通过、候选失败”的关键病例。

**1:15–2:10｜看证据**
进入病例详情，展示候选版漏掉的急症升级建议、自动证据和人工“确认问题”记录。

**2:10–2:40｜看门禁**
打开门禁报告：即使候选版总分更高，只要确认 P0 仍存在，结论就是 `BLOCKED`。

**2:40–3:00｜看发布连接**
展示三态退出码：`0=PASSED`、`1=BLOCKED`、`2=REVIEW_REQUIRED`。本仓库 CI 每次 push 都会跑离线回放并断言退出码**恰好为 1**（该拦没拦、不该拦却拦了，两种都会让 CI 红）；在目标仓库启用必要检查后，P0 会阻断合并。

## 我的工作与 AI 使用边界

- 我负责问题定义、范围取舍、信息架构、状态机、门禁规则、验收标准和后续代码实现。
- AI 用于资料检索、结构化初稿和对抗性审查；外部事实回到官方资料核验，最终取舍由我确认。
- 当前可以证明：T0 资产校验、固定 fixture 离线 Runner、前端到本地 FastAPI 的 live run 合同、无网络 Fake Client 全链路，以及 CI 在每次 push 上跑通离线门禁断言；真实 DeepSeek 已完成 15 次运行，但**精确 token 与费用尚未核算**（客户端未持久化上游 `usage`）。**不能声称**部署、压测或医疗验证已经完成。

## 继续阅读

- [需求方案 v0.2](./01_需求方案.md)
- [技术方案 v0.2](./02_技术方案.md)
- [v0.1 审核意见](./03_审核意见_20260812.md)
- [竞品与差异化](./04_竞品与差异化.md)
- [关键决策记录](./05_关键决策记录.md)
- [Agent 配置包与三类 Skill 真实评测方案](./09_Agent配置包与三类Skill评测方案_20260814.md)
