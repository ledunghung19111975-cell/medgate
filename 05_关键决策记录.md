# MedGate｜关键决策记录

> 这份记录解释我为什么这样设计，也记录每个选择主动放弃了什么。部分决策已实现，并完成无网络模拟与本地浏览器验收；真实 DeepSeek 上游已于 2026-08-14 起在本机完成多次真实 live run（证据见 `PROGRESS.md`）。状态证据以 `PROGRESS.md` 为准。

> 说明：下文“验证”字段同时包含已执行证据与待完成验收，不能仅凭标题判断状态；当前执行结果和剩余边界统一记录在 `PROGRESS.md`。

| 项目 | 内容 |
| --- | --- |
| 版本 | v0.2 |
| 日期 | 2026-08-12 |
| 决策状态 | 首发范围已冻结，允许在实现证据出现后修订 |

## D-01 首发做门禁演示，不做完整评测平台

**问题**：完整方案包含版本、测试集、病例 CRUD、导入、审计、趋势、双数据库和多种适配器，单人很可能在核心链路完成前耗尽时间。

**我考虑的选项**：

- 按完整平台从资产管理开始开发；
- 先做只读页面原型；
- 只实现面试官会走的门禁证据链。

**我的选择**：首发只保留总览、评测详情、病例详情、发布门禁 4 个页面（后于 T0 版本化资产阶段增加测评集详情页，现为五页面）。版本与病例由仓库文件和 seed 脚本装载。

**代价**：首版不能证明复杂后台 CRUD 和企业配置能力。

**验证**：公开链接能在 3 分钟内讲完“发现回退—查看证据—确认 P0—阻断 CI”。

## D-02 首发 12 例，不一次写满 30 例

**问题**：每个病例不仅要写病情，还要写多轮脚本、检查点、禁止行为、基线/候选对话和 Judge 证据；内容生产是关键路径。

**我的选择**：首发 12 例，其中 4 个关键病例、1 个候选版 P0 回退；跑通后再扩到 30 例。

**配套工具**：测试集与 fixture 的完整性校验由 `medgate/assets.py`（`python -m medgate validate`，含 manifest 哈希链）与 `scripts/validate_assets.mjs`（结构校验）承担；原设想的 `gen_fixture.py` / `validate_cases.py` 独立脚本未单独实现，fixture 为手工编写后经上述校验入库。工具不能替代医学判断，所有文本仍需人工逐条确认。

**代价**：覆盖度有限，不能据此声明模型医疗安全表现。

**验证**：12 例全部具有双版本输出、检查点证据和预期 gate 结果；Golden 测试可重复。

## D-03 公开访客只读，不做会话沙箱

**问题**：让匿名访客真实创建运行，需要数据隔离、限流、重置和写权限治理，与作品集价值不匹配。

**我的选择**：公开入口只展示一份预置、已复核、可追溯的 `BLOCKED` 运行。写操作只在本地或受保护的私人演示环境开放。

**代价**：访客不能亲手点击“开始评测”。

**补偿**：公开页面完整保留进度、差异、复核和 CI 证据；项目说明提供 3 分钟演示脚本。

## D-04 回放是一等执行模式，不是视觉 mock

**问题**：公开 Demo 不能依赖我的模型密钥，也不能因为外部服务限流而失效。

**我的选择**：Agent 输出和 Judge 证据都使用版本化 fixture，但仍经过同一套 schema 校验、评分、Finding 和 Gate 引擎。回放环境禁止非数据库外连。

**代价**：需要自己生产和维护 fixture；live 适配器虽已通过无网络 Fake Client 测试，仍不能证明用户账户下的真实上游输出、耗时和费用。

**验证**：无网络 E2E 完成全链路，`external_call_count=0`，结果哈希稳定。

## D-05 P0 否决权独立于加权总分

**问题**：平均分会把低频高风险错误稀释，出现“总分更高但关键病例更危险”。

**我的选择**：六维分数用于比较和定位；任何未解决候选版 P0 都产生 `BLOCKED`。总分、阈值或普通备注不能覆盖该结论。

**代价**：会出现总分较高却无法发布的反直觉结果，也会增加人工复核压力。

**验证**：Golden 数据中候选版总分高于基线但包含一个确认 P0，Gate 必须保持 `BLOCKED`。

## D-06 Gate 绑定 run，Finding 跨 run 闭环

**问题**：如果新 run 修复问题后把旧 run 改成绿色，历史发布判断就被改写；如果完全不关联，团队又不知道旧问题后来是否修好。

**我的选择**：

- GateDecision 绑定一次 run；同一 run 的人工事实纠错可以追加新 decision。
- 报告快照绑定确定的 decision，发布后不变。
- 新 run 回归通过只改变旧 Finding 的生命周期，不改变旧 run 的历史 Gate。

**代价**：需要两个相关但不同的状态模型，页面必须避免把“问题已关闭”误写成“旧版本当时可发布”。

**验证**：关联新 run 通过后，旧 Finding 显示“回归通过”，旧报告仍显示当时的 `BLOCKED`。

## D-07 门禁必须接 CI，但声明强制范围

**问题**：如果结论只停留在 Web 报告，它不能阻止任何发布。

**我的选择**：提供 CLI、Gate 查询接口和 GitHub Actions 示例：`0=PASSED`、`1=BLOCKED`、`2=REVIEW_REQUIRED`。私人复核通过绑定稳定内容哈希的 ReviewPack 带回离线 CI；待复核 P0 失败关闭。在仓库启用必要检查与分支保护后，非 PASSED 状态阻断普通合并。

**代价**：人工复核是异步的；首版 CI 不等待，而是失败关闭或返回 `REVIEW_REQUIRED`，复核后携带 ReviewPack 重新运行，不做复杂回调服务。ReviewPack 也不能替代真实身份和签名审批，Demo 仓库需要代码评审保护。

**边界**：仓库管理员可以关闭保护规则，因此“不允许绕过”只指产品规则和已配置的普通合并流程，不是对基础设施管理员的绝对承诺。

## D-08 自己实现策略层，通用能力不重复建设

**问题**：Promptfoo、DeepEval、Braintrust、Langfuse 已经覆盖通用评测、人工标注或 CI；从零复制它们没有价值。

**我的选择**：首发自己实现最小回放、评分、Finding 和 Gate，以证明核心编码；不做 Trace、Prompt 管理和指标生态。后续可以让 Promptfoo、DeepEval 或 Medmarks 成为执行适配器。

**代价**：首发集成少、规模小，也无法享受成熟工具的生态。

**验证**：内部协议不依赖某个第三方数据结构；替换 runner 不改变 Gate 与报告模型。

## D-09 轻量原型优先，设计投入放在证据表达

**问题**：自建表单、Dialog、Toast、Table、Skeleton 会消耗大量时间，但不是作品的核心差异。

**我的选择**：原型最终采用**零依赖的单文件 HTML/CSS/JS**（`prototype/index.html`，无框架、无构建步骤）。设计阶段评估过 React + TypeScript + shadcn/ui，为保持「clone 即开即用、不装 npm 依赖」而弃用——配套静态校验脚本（`scripts/check_prototype.mjs` 等）也只用 node 内置模块。

**代价**：没有组件库现成的可访问性与交互基线，键盘可完成主链路、焦点管理、状态着色冗余需自行实现与检查。

**验证**：键盘可完成主链路；严重度和 Gate 不只依赖颜色；五个页面视觉一致（结构断言由 `scripts/check_prototype.mjs` 覆盖）。

## D-10 诚实披露医疗与实现边界

**问题**：作品集最容易把设计目标写成已实现效果，或把合成病例包装成临床验证。

**我的选择**：所有材料明确区分“已核实事实、方案判断、计划能力和已知限制”。病例与标注由本人编写，未经执业医师复核；代码、部署、性能、CI 和医疗效果均要在真实验证后才能写成成果。

**代价**：材料不会出现夸张的性能和临床效果数字。

**验证**：项目说明、需求、技术、竞品和 README 中不存在未实现能力的完成式表述。

## D-11 手动提示词 live run 与公开回放隔离

**问题**：固定 fixture 可以证明门禁机制，却不能证明它绑定了用户实际想发布的提示词；若把实时调用直接塞进既有回放，又会破坏公开 Demo 的零密钥与可复现性。

**我的选择**：保留 `/api/v1/runs` 与 `medgate run` 的纯回放语义，新增本机专用 `/api/v1/live-runs`。操作者手动输入 Baseline/Candidate 提示词，模型固定为 `deepseek-v4-flash`；服务端生成回答与 Judge，确定性规则再次扫描原文。报告绑定提示词全文与哈希、模型参数、实时证据哈希和 `run_input_hash`。公开模式完全不读取 live state。

**安全边界**：API 默认只绑定 `127.0.0.1`；Key 只从 `DEEPSEEK_API_KEY` 读取，不进入 HTML、请求体、日志或仓库。模型错误、空回答、Judge 解析失败和低置信度均失败关闭，不能默认 `PASSED`。

**代价**：完整运行是 50 次同步调用，可能耗时并产生费用；当前不做异步进度、token/费用仪表盘和失败项断点续跑。

**验证**：无网络 Fake Client 覆盖 50 次编排、多轮上下文、提示词改字改变哈希、旧 fixture 不参与 live 判定、幂等重放、脱敏错误与 Judge 失败关闭；真实 DeepSeek 结果须在用户本机注入 Key 后另行记录。

## D-12 分层门禁：边界层零容忍走现有 P0 语义，其余三层先只出分不判

**问题**：测试集要按场景分四层（FAQ／复杂疾病／多轮／边界），那门禁要不要也分层？如果分层，每层的通过线定多少？

**我考虑的选项**：

- 每层各设一个通过率阈值（如 FAQ ≥95%、边界 ≥99%），门禁按层判；
- 不分层，继续只按严重度全局判；
- 只有边界层设硬门禁，其余三层先出分不判，等实测分布出来再定线。

**我的选择**：第三种。**边界层零容忍——任何一条严重违规即 BLOCKED**，形态借自 NOHARM 的 `Severe_rate`（出现至少一条 Severe 危害的样本占比）。其余三层（FAQ／复杂疾病／多轮）**报告按层出分，但不参与门禁判定**，等 P1 首轮 150 条真实运行产出实测分布后，再由我按分布定阈值。

**关键简化**：边界层零容忍**不需要新的门禁逻辑**。现有 Gate 已经是「任何未关闭的 P0 → BLOCKED」，所以只要把边界层的严重违规**标为 P0**，现有判定就直接生效。新增的只有报告侧的分层聚合，不动 `engine.py` 的 Gate 分支。

**为什么其余三层不先定线**：在没有基线分布之前拍阈值，拍出来的一定是错的——要么松到形同虚设，要么紧到每次都红。NOHARM 自己也是先有 330 个样本的实测才给出参考表。

**代价**：P1 交付时只有边界层是真门禁，其余三层的分数暂时只能看不能卡；分层阈值要等到 P2 首轮跑完才补齐，中间存在一个「有数据没门禁」的窗口。

**判定标准来源**：`12_医疗测评集选型调研_20260817.md` §七（HealthBench 的 rubric 正负分与按 tag 出分、NOHARM 的严重度加权与 `Severe_rate`、CMB-Clin 的四维 1–5 分）。
