# 提示词版本管理模板（V1→V2→V3 迭代闭环）

| 项目 | 内容 |
| --- | --- |
| 用途 | 把提示词迭代从一次性修修补补升级为可复现、可审计、可展示的资产；每个版本都能回答"改了什么 / 为什么改 / 怎么验证 / 结果如何 / 留下什么" |
| 适用范围 | MedGate 项目内提示词迭代；结构可复用到其他 Agent/提示词评测场景（通用件） |
| 关联资产 | `08_药物咨询双版本自测提示词_20260812.md`（V1/V2 全文）、`10_提示词V3迭代_20260817.md`（V3 全文 + 实测结果） |
| 当前版本演进 | V1 → V2 → V3（三版均已闭环，见下方总览与索引） |

---

## 一、版本演进总览

| 版本 | 关键改动（单变量） | 动机（bad case 归因） | 验证结果 | 状态 |
| --- | --- | --- | --- | --- |
| V1 | 基线：真实检索 + 逐条引用（无固定结构） | — | 8 例 P0/P1 全 100 分；行为稳定 | 已闭环（Baseline 对照组） |
| V2 | 仅增加固定输出结构（一句话总结/关键问题概述/你可以怎么做/参考来源） | 统一展示形态、降低结构漂移 | 稳定实现固定结构；综合质量未显著优于 V1（不宣称提升） | 已闭环（2026-08-14 自测） |
| V3 | 仅 R4 追加"症状类咨询必须追问起病时间，只问发作时长不替代" | case-007 bad case：候选版只问"持续多久"不问"何时开始"，规则层 `ask_onset_time` + Judge 双层 fail | case-007 Baseline 0 分 → Candidate 100 分；12/12 全绿无回归；0 findings | 已闭环（run-20260817-144511） |
| 下一版 | 候选方向：复审语义等价修复（case-002 误报已在本轮代码层修复，见 10_ 文档"下一轮迭代方向"） | — | — | 待启动 |

---

## 二、迭代模板（每个新版本按此七步闭环）

### 第 0 步：登记版本与边界

- 版本号、日期、目标（这个版本要解决什么 bad case / 什么质量问题）。
- 声明实验边界：单变量、数据边界、医学未复核。

### 第 1 步：Bad case 归因（先归因，再动手）

- 写清 bad case 的 case_id、优先级、检查点、表现（哪一层 fail：规则层 / Judge / 复审）。
- 给出模型实际输出片段，指出具体缺陷。
- 定位根因：是提示词约束缺失、规则词表过窄、Judge 提示词漂移，还是复审裁决逻辑问题——**区分"模型行为不可控"与"裁决逻辑可控"**，修可控的一侧。

### 第 2 步：单变量改动

- 只改一处；明确"改动点"表格（位置 / 旧 / 新），其余全文逐字不变，保证 A/B 可精确归因。
- 改动类型三选一：
  - 提示词约束（如 V3 的 R4）
  - 规则词表（如 avoid_self_driving 语义等价扩展）
  - 复审/Judge 提示词契约（如语义等价裁决）

### 第 3 步：写验证预期（可复验、可量化）

- 预期 1：bad case 从 fail → pass（给出判定依据：命中哪个规则 / Judge 认可）
- 预期 2：其余用例无回归（同一测试集重跑，不允许新增 fail）
- 预期 3：若新增 fail 的归因流程（规则误报 / 模型缺陷 / Judge 漂移）

### 第 4 步：真实运行

- 在 MedGate 工作台页面粘贴 Baseline/Candidate 两版提示词，跑同一测试集。
- 记录 run_id、外调次数、Gate 状态。

### 第 5 步：回填实测结果

- 按预期逐条核对（✅/❌），附 run_id、关键对比数字、附带发现（可能存在的其他误报/漏报）。
- 记录遗留问题与下一轮方向，保证闭环可接力。

### 第 6 步：回归固化（防止回退）

- 本轮相关 bad case 与新增表达进测试（规则匹配单测 / 复审 prompt 契约单测 / 场景集成测试）。
- 规则或协议变更时同步重绑定 ReviewPack 的 rule_hash / run_input_hash（规则版本升级，旧复核声明随之失效是预期行为）。

---

## 三、已落地实例索引

| 版本 | 文档 | 关键证据 |
| --- | --- | --- |
| V1 / V2 | `08_药物咨询双版本自测提示词_20260812.md` | 双版本全文、自测结果（2026-08-14） |
| V3 | `10_提示词V3迭代_20260817.md` | bad case 归因、唯一改动点、验证预期、实测结果（run-20260817-144511）、case-002 误报发现与下一轮方向 |
| case-002 修复 | 代码：`medgate/engine.py`（词表）、`medgate/live.py`（复审语义等价 + verification 字段一致性）；提交 `79c821b` | 真实模型定向验证：复审对 case-002 原始回答从 fail → pass |

---

## 四、使用说明（新迭代怎么走）

1. 新 bad case 出现 → 从本文"二、迭代模板"复制结构，新建 `NN_提示词Vx迭代_YYYYMMDD.md`（编号延续项目文档体系）。
2. 严格执行 0→6 步；第 5 步未回填前，该版本不算闭环，不进入下一版。
3. 每版结束更新"一、版本演进总览"，保证总览是唯一入口、详情在版本文档。
4. 遗留问题不留在对话里，必须写进版本文档的"遗留问题与下一轮方向"——后续会话或面试复盘可直接接力。

---

## 五、边界与注意

- **单变量是硬约束**：同一版本只改一处；多处改动无法归因，等于没做实验。
- **失败关闭语义不可破坏**：语义层（Judge/复审）判定 fail 时必须 fail；"语义等价放宽"只作用于规则层的字面误报，不改变禁止性红线的严格性。
- **词表永远修不满**：规则层词表只是第一道门，语义等价由复审裁决兜底；不要用逐条扩词表代替职责切分。
- **医学未复核**：合成病例、提示词、规则与判定均未经执业医师复核，仅用于产品机制自测与作品集展示，不用于临床决策。